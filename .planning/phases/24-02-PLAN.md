---
phase: 24
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified: [apps/vibe-cli/src/main.rs]
autonomous: true
requirements: [REL-05]
must_haves:
  truths:
    - "vibe 命令能根据子命令动态分发至 plugin/vibe/scripts/ 下的 JS 脚本"
    - "分发逻辑正确传递了环境变量（如 VIBE_MASTER_ID）及所有 CLI 参数"
    - "脚本执行失败时，Rust CLI 能捕获非零退出码并正常报错"
  artifacts:
    - path: "apps/vibe-cli/src/main.rs"
      provides: "轻量级 Plugin 分发器入口"
---

<objective>
重构 Rust CLI 入口，使其转变为轻量级的插件分发器，将核心业务逻辑剥离。

Purpose: 实现 v6.0 的轻量化 Rust 边界，提升系统灵活性和可维护性。
Output: 瘦身后的 `main.rs`。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-CONTEXT.md
@.planning/phases/24-RESEARCH.md
@.planning/phases/24-VALIDATION.md
@apps/vibe-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 main.rs 分发逻辑</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    - 修改 `Cli` 结构体，支持 `clap` 的 `external_subcommand` 以接收任意子命令。
    - 保留基础环境检测（如 `detect_current_terminal`），并将结果导出为环境变量。
    - 实现脚本查找逻辑：
        - 优先检查 `.vibe/scripts/<cmd>.js`。
        - 备选检查 `plugin/vibe/scripts/<cmd>.js`。
    - 实现 Node.js 调用逻辑：
        - 使用 `tokio::process::Command` 调用 `node`。
        - 传递所有子命令后的 args。
        - 设置 `VIBE_MASTER_ID` 环境变量。
    - 移除原本硬编码的业务子命令（如 `spawn`, `kill` 等，这些功能已由脚本提供）。
  </action>
  <verify>
    <automated>cargo build && target/debug/vibe release-summary --json</automated>
  </verify>
  <done>vibe 入口成功分发请求至 release-summary.js 脚本并获得预期输出。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI -> Subprocess | 启动 Node.js 进程执行外部脚本 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Elevation of Privilege | Dispatcher | mitigate | 严格限制脚本搜索路径，仅允许执行 plugin 目录下的脚本 |
| T-24-04 | Denial of Service | Subprocess | mitigate | 处理脚本挂起情况，必要时设置超时 |
</threat_model>

<verification>
1. 编译并运行 `vibe release-summary` 检查是否成功触发 24-01 实现的脚本。
2. 运行 `vibe unknown-cmd` 验证是否正确提示脚本未找到。
</verification>

<success_criteria>
- Rust CLI 的核心功能变为“路径查找 + 进程启动”。
- 业务逻辑与 Rust 二进制完全解耦。
</success_criteria>

<output>
After completion, create .planning/phases/24-cli-slimming-p1/24-02-SUMMARY.md
</output>
