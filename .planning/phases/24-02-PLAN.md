---
phase: 24
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified: [apps/vibe-cli/src/main.rs]
autonomous: true
requirements: [REL-05]
must_haves:
  truths:
    - "vibe 命令可以正确识别并调用 plugin/vibe/scripts/ 下的 JS 脚本"
    - "调用脚本时能正确传递命令行参数"
    - "vibe-cli 仍能正确设置 VIBE_MASTER_ID 等环境变量供脚本使用"
  artifacts:
    - path: "apps/vibe-cli/src/main.rs"
      provides: "轻量级命令分发器逻辑"
---

<objective>
将 `vibe-cli` 重构为轻量级分发器，将业务逻辑下沉至插件脚本。

Purpose: 实现 v6.0 的插件化架构，减少 Rust 二进制体积，提高逻辑灵活性。
Output: 瘦身后的 `main.rs`。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-CONTEXT.md
@.planning/phases/24-RESEARCH.md
@apps/vibe-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 main.rs 为分发器</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    - 修改 `Cli` 结构体，支持解析动态子命令（例如使用 `clap` 的 `external_subcommand` 或通配符模式）。
    - 保留环境检测逻辑（`detect_current_terminal`），并根据结果设置 `VIBE_MASTER_ID` 环境变量。
    - 实现分发逻辑：
        1. 获取子命令名称。
        2. 搜索路径优先级：`.vibe/scripts/<cmd>.js` -> `plugin/vibe/scripts/<cmd>.js`。
        3. 如果找到匹配脚本，使用 `tokio::process::Command` 调用 `node <script_path> <args>`。
        4. 如果未找到，检查是否为内置命令（如 `version`, `help`），否则报错。
    - 移除原本硬编码在 `main.rs` 中的复杂业务逻辑（如 `Spawn`, `Split`, `Kill` 等的具体实现），这些逻辑应已在 Phase 22/23 中迁移至脚本。
  </action>
  <verify>
    <automated>cargo build && target/debug/vibe release-summary --json</automated>
  </verify>
  <done>vibe 命令成功调用 release-summary.js 并输出结果。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI -> Node.js | 执行外部 JS 脚本 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Elevation of Privilege | main.rs dispatcher | mitigate | 仅执行指定路径下的 .js 脚本，验证脚本路径合法性 |
| T-24-04 | Denial of Service | main.rs dispatcher | mitigate | 处理子进程执行超时或异常退出 |
</threat_model>

<verification>
1. 运行 `vibe release-summary` 验证是否成功调用之前实现的脚本。
2. 运行 `vibe status` 验证是否调用了 `plugin/vibe/scripts/status.js`。
</verification>

<success_criteria>
- `main.rs` 代码量显著减少，主要逻辑为路径查找和进程启动。
- 所有现有的核心命令均能通过分发机制正常工作。
</success_criteria>

<output>
After completion, create .planning/phases/24-cli-slimming-p1/24-02-SUMMARY.md
</output>
