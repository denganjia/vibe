---
phase: "26-skill-standardization"
plan: "01"
type: "tdd"
wave: 1
depends_on: []
files_modified: 
  - "plugin/vibe/package.json"
  - "plugin/vibe/package-lock.json"
  - "plugin/vibe/scripts/skills.test.js"
autonomous: true
requirements: 
  - "SKL-01"
  - "SKL-02"
must_haves:
  truths:
    - "SKILL.md frontmatter is automatically validated"
    - "Legacy structure is completely removed"
    - "Package configuration includes skills validation dependencies and scripts"
  artifacts:
    - path: "plugin/vibe/scripts/skills.test.js"
      provides: "Validation for SKILL.md frontmatter and directory structure"
      exports: []
  key_links:
    - from: "plugin/vibe/package.json"
      to: "plugin/vibe/scripts/skills.test.js"
      via: "npm run test"
      pattern: "test:skills"
---

<objective>
Setup TDD validation for skill standardization.
Purpose: Use `js-yaml` to robustly validate that all skills have standard YAML frontmatter and conform to the new directory structure.
Output: A new test script and updated `package.json`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-skill-standardization/26-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependencies and test scripts</name>
  <files>plugin/vibe/package.json, plugin/vibe/package-lock.json</files>
  <read_first>
    - plugin/vibe/package.json
  </read_first>
  <action>
    Update `plugin/vibe/package.json`:
    1. Add `"js-yaml": "^4.1.1"` to `devDependencies`.
    2. Add `"test:skills": "node --test scripts/skills.test.js"` to the `scripts` object.
    3. Update the `"test"` script to `"npm run test:manifests && npm run test:skills"`.
    Run `npm install` from the `plugin/vibe/` directory to update `package-lock.json`.
  </action>
  <acceptance_criteria>
    - `cat plugin/vibe/package.json` contains `"test:skills": "node --test scripts/skills.test.js"`
    - `cat plugin/vibe/package.json` contains `"js-yaml": "^4.1.1"` in `devDependencies`
    - `ls -l plugin/vibe/node_modules/js-yaml` exits 0 (folder exists)
  </acceptance_criteria>
  <verify>
    <automated>cd plugin/vibe && npm list js-yaml</automated>
  </verify>
  <done>package.json is configured and dependencies installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TDD validation script</name>
  <files>plugin/vibe/scripts/skills.test.js</files>
  <read_first>
    - plugin/vibe/scripts/manifests.test.js
  </read_first>
  <action>
    Create `plugin/vibe/scripts/skills.test.js` using `node:test`, `node:assert/strict`, `node:fs`, `node:path`, and `js-yaml`. 
    Implement two tests:
    1. "SKILL.md files have valid frontmatter": Read all directories inside `plugin/vibe/skills/`. For each directory, assert `SKILL.md` exists. Extract frontmatter using regex `/^\s*---\n([\s\S]*?)\n---/` and parse it using `yaml.load()`. Assert `name`, `version`, and `description` are truthy in the frontmatter.
    2. "Legacy files and directories are removed": Assert `fs.existsSync` is false for `plugin/vibe/commands`, `plugin/vibe/roles`, and `plugin/vibe/skills/Conductor.md`.
    Note: These tests will fail until Plan 26-03 completes migration.
  </action>
  <acceptance_criteria>
    - `cat plugin/vibe/scripts/skills.test.js` contains `require('js-yaml')`
    - `cat plugin/vibe/scripts/skills.test.js` contains regex `/^\s*---\n([\s\S]*?)\n---/`
    - `node --test plugin/vibe/scripts/skills.test.js` executes without syntax errors (test failures are expected)
  </acceptance_criteria>
  <verify>
    <automated>node --test plugin/vibe/scripts/skills.test.js || true</automated>
  </verify>
  <done>Validation script exists and asserts the required schema and structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File parsing | js-yaml parsing local files |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | skills.test.js | mitigate | Use js-yaml safe load (default in v4) to prevent arbitrary code execution from YAML frontmatter |
</threat_model>

<verification>
- package.json is updated
- tests run via `npm run test:skills`
</verification>

<success_criteria>
- Dependencies installed
- `skills.test.js` created and syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/26-skill-standardization/26-01-SUMMARY.md`
</output>
