---
phase: "26-skill-standardization"
plan: "03"
type: "execute"
wave: 3
depends_on: ["02"]
files_modified: 
  - "plugin/vibe/roles/Conductor.md"
  - "plugin/vibe/skills/Conductor.md"
  - "plugin/vibe/skills/conductor/SKILL.md"
  - "plugin/vibe/skills/plan/SKILL.md"
  - "plugin/vibe/README.md"
autonomous: true
requirements: 
  - "SKL-01"
  - "SKL-02"
must_haves:
  truths:
    - "Conductor skill is defined in skills/conductor/SKILL.md with proper YAML frontmatter"
    - "Legacy roles directory is removed"
    - "Internal documentation paths are updated to point to the new locations"
  artifacts:
    - path: "plugin/vibe/skills/conductor/SKILL.md"
      provides: "Conductor role definition"
      exports: []
  key_links:
    - from: "plugin/vibe/README.md"
      to: "plugin/vibe/skills/"
      via: "Markdown text links"
      pattern: "skills/"
---

<objective>
Migrate legacy `roles/` into standardized `skills/` structure and update documentation.
Purpose: Comply with the unified plugin standard matching the package.json identity (SKL-01, SKL-02).
Output: Standardized Conductor `SKILL.md`, deleted legacy roles directory, and updated internal links.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-skill-standardization/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Conductor role to standardized skill</name>
  <files>plugin/vibe/roles/Conductor.md, plugin/vibe/skills/Conductor.md, plugin/vibe/skills/conductor/SKILL.md, plugin/vibe/roles/</files>
  <read_first>
    - plugin/vibe/roles/Conductor.md
    - plugin/vibe/skills/Conductor.md
  </read_first>
  <action>
    1. Consolidate Conductor files: Append the non-redundant content of `plugin/vibe/roles/Conductor.md` and `plugin/vibe/skills/Conductor.md` into `plugin/vibe/skills/conductor/SKILL.md`. Ensure the frontmatter in `plugin/vibe/skills/conductor/SKILL.md` explicitly includes `name: vibe-conductor`, `version: 0.1.0`, and a `description`.
    2. Delete `plugin/vibe/roles/` directory and `plugin/vibe/skills/Conductor.md`.
  </action>
  <acceptance_criteria>
    - `ls plugin/vibe/roles` exits 1 (directory does not exist)
    - `ls plugin/vibe/skills/Conductor.md` exits 1 (file does not exist)
    - `cat plugin/vibe/skills/conductor/SKILL.md` contains `version: 0.1.0`
  </acceptance_criteria>
  <verify>
    <automated>test ! -d plugin/vibe/roles && test -f plugin/vibe/skills/conductor/SKILL.md</automated>
  </verify>
  <done>Conductor files are merged and legacy files are deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Update documentation links</name>
  <files>plugin/vibe/skills/plan/SKILL.md, plugin/vibe/README.md</files>
  <read_first>
    - plugin/vibe/skills/plan/SKILL.md
    - plugin/vibe/README.md
  </read_first>
  <action>
    1. In `plugin/vibe/skills/plan/SKILL.md`, replace instances of `plugin/vibe/roles/Conductor.md` and `plugin/vibe/skills/Conductor.md` with `plugin/vibe/skills/conductor/SKILL.md`.
    2. In `plugin/vibe/README.md`, change `- commands/ will expose workflow entry contracts.` to `- skills/ will expose workflow entry contracts.`
  </action>
  <acceptance_criteria>
    - `cat plugin/vibe/README.md` contains `skills/ will expose workflow entry contracts`
    - `cat plugin/vibe/skills/plan/SKILL.md` contains `plugin/vibe/skills/conductor/SKILL.md`
  </acceptance_criteria>
  <verify>
    <automated>cd plugin/vibe && npm run test:skills</automated>
  </verify>
  <done>Documentation links are correct, and all TDD validations pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Documentation mapping | Internal file linking |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Information Disclosure | SKILL.md docs | accept | No sensitive information exposed; files are static project documents |
</threat_model>

<verification>
- `npm run test:skills` passes without errors.
- `roles/` is completely removed.
</verification>

<success_criteria>
- All SKL-01 and SKL-02 requirements are met.
- The standard `js-yaml` validation is green.
</success_criteria>

<output>
After completion, create `.planning/phases/26-skill-standardization/26-03-SUMMARY.md`
</output>
