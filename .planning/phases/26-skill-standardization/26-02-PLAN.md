---
phase: "26-skill-standardization"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01"]
files_modified: 
  - "plugin/vibe/commands/init.md"
  - "plugin/vibe/commands/plan.md"
  - "plugin/vibe/commands/release-summary.md"
  - "plugin/vibe/commands/review-task.md"
  - "plugin/vibe/commands/run-task.md"
  - "plugin/vibe/commands/status.md"
  - "plugin/vibe/skills/init/SKILL.md"
  - "plugin/vibe/skills/plan/SKILL.md"
  - "plugin/vibe/skills/release-summary/SKILL.md"
  - "plugin/vibe/skills/review-task/SKILL.md"
  - "plugin/vibe/skills/run-task/SKILL.md"
  - "plugin/vibe/skills/status/SKILL.md"
autonomous: true
requirements: 
  - "SKL-01"
  - "SKL-02"
must_haves:
  truths:
    - "All command skills are defined in skills/<name>/SKILL.md with proper YAML frontmatter"
    - "Legacy commands directory is removed"
  artifacts:
    - path: "plugin/vibe/skills/init/SKILL.md"
      provides: "Init skill definition"
      exports: []
  key_links:
    - from: "plugin/vibe/skills/init/SKILL.md"
      to: "plugin/vibe/scripts/skills.test.js"
      via: "validation"
      pattern: "name: vibe-init"
---

<objective>
Migrate legacy `commands/` into standardized `skills/` structure with YAML frontmatter.
Purpose: Comply with the unified plugin standard matching the package.json identity (SKL-01, SKL-02).
Output: Standardized `SKILL.md` files for commands and deleted legacy commands directory.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-skill-standardization/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate init, plan, and release-summary to standardized skills</name>
  <files>plugin/vibe/commands/init.md, plugin/vibe/commands/plan.md, plugin/vibe/commands/release-summary.md, plugin/vibe/skills/init/SKILL.md, plugin/vibe/skills/plan/SKILL.md, plugin/vibe/skills/release-summary/SKILL.md</files>
  <read_first>
    - plugin/vibe/commands/init.md
    - plugin/vibe/commands/plan.md
    - plugin/vibe/commands/release-summary.md
  </read_first>
  <action>
    For each command file (`init`, `plan`, `release-summary`), move its content into a new file at `plugin/vibe/skills/<name>/SKILL.md`. Prepend the content with the following exact YAML frontmatter:
    ```yaml
    ---
    name: vibe-<name>
    version: 0.1.0
    description: Execute the <name> workflow logic.
    ---
    ```
    *(Replace `<name>` with the corresponding command name, e.g., `init`)*
  </action>
  <acceptance_criteria>
    - `cat plugin/vibe/skills/init/SKILL.md` contains `name: vibe-init`
    - `ls plugin/vibe/commands/init.md` exits 1 (file does not exist)
  </acceptance_criteria>
  <verify>
    <automated>test -f plugin/vibe/skills/init/SKILL.md</automated>
  </verify>
  <done>Init, plan, and release-summary are migrated to skills.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate review-task, run-task, status to standardized skills and cleanup</name>
  <files>plugin/vibe/commands/review-task.md, plugin/vibe/commands/run-task.md, plugin/vibe/commands/status.md, plugin/vibe/skills/review-task/SKILL.md, plugin/vibe/skills/run-task/SKILL.md, plugin/vibe/skills/status/SKILL.md, plugin/vibe/commands/</files>
  <read_first>
    - plugin/vibe/commands/review-task.md
    - plugin/vibe/commands/run-task.md
    - plugin/vibe/commands/status.md
  </read_first>
  <action>
    For each command file (`review-task`, `run-task`, `status`), move its content into a new file at `plugin/vibe/skills/<name>/SKILL.md`. Prepend the content with the exact YAML frontmatter (same as Task 1).
    
    After migrating these remaining 3 commands, delete the entire `plugin/vibe/commands` directory.
  </action>
  <acceptance_criteria>
    - `cat plugin/vibe/skills/run-task/SKILL.md` contains `name: vibe-run-task`
    - `ls plugin/vibe/commands` exits 1 (directory does not exist)
  </acceptance_criteria>
  <verify>
    <automated>test ! -d plugin/vibe/commands && test -f plugin/vibe/skills/run-task/SKILL.md</automated>
  </verify>
  <done>All commands are migrated and the legacy directory is deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File system | Reorganizing static markdown files |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Information Disclosure | SKILL.md docs | accept | No sensitive information exposed; files are static project documents |
</threat_model>

<verification>
- `commands/` directory is completely removed.
- 6 command skills exist in `skills/` with proper frontmatter.
</verification>

<success_criteria>
- Command migration complete.
</success_criteria>

<output>
After completion, create `.planning/phases/26-skill-standardization/26-02-SUMMARY.md`
</output>
