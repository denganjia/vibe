---
phase: 28-workflow-alignment
plan: 01
type: execute
wave: 1
depends_on: ["27-mcp-integration"]
files_modified: [
  ".vibe/roles/Conductor.md",
  ".vibe/roles/Worker.md",
  "README.md",
  ".planning/codebase/ARCHITECTURE.md",
  ".planning/codebase/STRUCTURE.md"
]
autonomous: true
requirements: ["WF-01", "WF-02"]
must_haves:
  truths:
    - "Conductor SOP 明确指示使用 vibe_create_task 和 vibe_get_status"
    - "Worker SOP 明确指示使用 vibe_acquire_lock 进行冲突预防"
    - "ARCHITECTURE.md 准确描述了基于 MCP 的插件架构，不再提及 Rust 守护进程"
  artifacts:
    - path: ".vibe/roles/Conductor.md"
      provides: "Updated orchestration guidelines"
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "New system architecture definition"
---

<objective>
更新 AI Conductor 和 Worker 的 SOP 及其相关架构文档，使之全面支持并优先使用 Phase 27 引入的 MCP 工具集。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/28-RESEARCH.md
@plugin/vibe/mcp-server.js
@.vibe/roles/Conductor.md
@.vibe/roles/Worker.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新角色 SOP (Conductor & Worker)</name>
  <files>.vibe/roles/Conductor.md, .vibe/roles/Worker.md</files>
  <action>
    1. 修改 `Conductor.md`：
       - 将 `vibe spawn` 替换为 `vibe_create_task`。
       - 将 `vibe wait` 和 `vibe list` 替换为 `vibe_get_status` 和 `vibe_list_tasks`。
       - 引入 `vibe_skill_plan` 和 `vibe_skill_release_summary` 作为标准阶段工具。
    2. 修改 `Worker.md`：
       - 将 `vibe report` (intent locking) 替换为 `vibe_acquire_lock`。
       - 明确要求在任务完成后使用 `vibe_release_lock`。
       - 更新状态汇报逻辑，建议使用 MCP 工具或特定的技能工具。
  </action>
  <verify>
    <manual>检查 SOP 文件，确认所有旧的 'vibe' shell 命令已被对应的 MCP 工具调用替代。</manual>
  </verify>
  <done>SOP 现在指示 Agent 使用更安全、更结构化的 MCP 工具。</done>
</task>

<task type="auto">
  <name>Task 2: 更新架构与结构文档</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, README.md</files>
  <action>
    1. 重写 `ARCHITECTURE.md`：
       - 移除 Rust `vibe-cli` 和 `vibe-core` 的分层描述。
       - 引入 MCP 服务层作为核心调度器。
       - 描述 "Plugin-first" 模式下的数据流（Host CLI -> MCP Server -> Scripts -> .vibe/）。
    2. 更新 `STRUCTURE.md`：
       - 标注 `crates/` 和 `apps/vibe-cli/` 为 Archive/Legacy。
       - 强调 `plugin/vibe/` 为项目的活跃核心。
    3. 更新 `README.md`：
       - 更新安装和使用说明，反映 MCP 插件的安装方式。
       - 强调与 Gemini, Claude, Codex 的通用兼容性。
  </action>
  <verify>
    <manual>阅读文档，确保架构描述与代码库的实际 v7.0 状态一致。</manual>
  </verify>
  <done>项目文档准确反映了新的插件架构。</done>
</task>

<task type="auto">
  <name>Task 3: 最终验证与清理</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    1. 在 `ROADMAP.md` 中标记 Phase 28 为已完成。
    2. 在 `REQUIREMENTS.md` 中更新 WF-01 和 WF-02 的状态为已完成。
    3. 检查全量文档中是否存在指向已删除或已弃用 Rust 组件的死链接或误导性描述。
  </action>
  <verify>
    <automated>grep -r "vibe-cli" . (排除 archive/ 和 legacy 标注)</automated>
  </verify>
  <done>Phase 28 目标达成，项目状态同步更新。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- AI Agent → MCP Tooling: AI Agent 现在通过受限的工具集而非任意 shell 命令与工作区交互。

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | Documentation | mitigate | 确保文档中不包含敏感路径或硬编码的本地配置。 |
| T-28-02 | Denial of Service | Locking SOP | mitigate | 强调 `vibe_release_lock` 的必要性，防止 Agent 因异常退出导致资源永久锁定。 |
</threat_model>

<verification>
验证所有修改后的 MD 文件均符合 Markdown 规范。
确保 Conductor 在模拟环境中能通过 `list_tools` 发现所有提到的工具。
</verification>

<success_criteria>
1. SOP 完全转向 MCP 工具链。
2. 架构文档与 v7.0 插件优先架构完全同步。
3. `README.md` 提供了清晰的 MCP 插件接入指南。
</success_criteria>

<output>
After completion, create `.planning/phases/28-SUMMARY.md`
</output>
