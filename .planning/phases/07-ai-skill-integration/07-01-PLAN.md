---
phase: 07-ai-skill-integration
plan: 01
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/vibe-cli/src/main.rs
  - apps/vibe-cli/Cargo.toml
  - crates/vibe-core/src/env.rs
  - crates/vibe-core/src/state/db.rs
autonomous: true
requirements: ["SKILL-01", "SKILL-02"]

must_haves:
  truths:
    - "vibe-check returns structured JSON explaining environment support"
    - "vibe-cli supports an MCP mode (stdio-based) for Claude/Cursor"
    - "AI can trigger external WezTerm panes from non-supported terminals (like VSCode)"
  artifacts:
    - path: "apps/vibe-cli/src/mcp.rs"
      provides: "MCP Server implementation"
    - path: "apps/vibe-cli/src/main.rs"
      provides: "vibe check command"
---

<objective>
将 vibe-cli 封装为符合 MCP 标准的 AI 技能集，并增强环境自检能力，确保 AI 能在各种终端环境下（包括 VSCode 内部）安全、智能地编排物理空间。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 实现环境自检指令 vibe-check</name>
  <files>apps/vibe-cli/src/main.rs, crates/vibe-core/src/env.rs</files>
  <action>
    1. 在 `vibe-cli` 中新增 `vibe check` 子命令。
    2. 实现 JSON 输出结构，包含：
       - `supported`: 当前终端是否支持 split/focus。
       - `terminal_type`: 探测到的终端名称。
       - `master_alive`: Master 服务连接状态。
       - `active_workers`: 当前活跃的 Agent 数量。
       - `recommendation`: 给 AI 的下一步操作建议。
  </action>
  <verify>
    <automated>vibe check --json (在 WezTerm 和普通终端中对比输出结果)</automated>
  </verify>
  <done>AI 现在有了“起飞前检查”的能力，能根据环境决定是否切换到外部编排模式。</done>
</task>

<task type="auto">
  <name>Task 2: 搭建 MCP Server 核心框架</name>
  <files>apps/vibe-cli/Cargo.toml, apps/vibe-cli/src/mcp.rs, apps/vibe-cli/src/main.rs</files>
  <action>
    1. 引入 `mcp-sdk-rs`（或手动实现标准 stdio 协议）。
    2. 新增 `vibe mcp` 子命令，启动标准输入输出流服务。
    3. 定义 `list_tools` 响应，暴露：`vibe_check`, `vibe_run`, `vibe_split`, `vibe_inject`, `vibe_focus`。
  </action>
  <verify>
    <manual>使用 mcp-inspector 或 Claude Desktop 链接 vibe mcp 并列出工具。</manual>
  </verify>
  <done>vibe 现已成为一个标准的 MCP 能力节点。</done>
</task>

<task type="auto">
  <name>Task 3: 实现“外部编排”智能路由 (Remote-Control Mode)</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    1. 优化 `split` 和 `run` 逻辑：当探测到不支持当前终端切分时，自动回退到“寻找/启动外部终端（WezTerm）”模式。
    2. 确保 AI 在 VSCode 终端调用 `vibe split` 时，屏幕上能弹出一个 WezTerm 窗格来承载任务。
  </action>
  <verify>
    <manual>在普通 macOS Terminal 中运行 vibe split，观察是否能自动唤起 WezTerm 并在其中创建窗格。</manual>
  </verify>
  <done>打破了 AI 必须在 WezTerm 内部运行的限制，实现了真正的全局编排。</done>
</task>

<task type="auto">
  <name>Task 4: AI 上下文感知增强</name>
  <files>crates/vibe-core/src/state/db.rs, apps/vibe-cli/src/main.rs</files>
  <action>
    1. 在 `vibe status --json` 中增加更多字段：工作目录、最近产生的日志片段。
    2. 为 MCP 提供 `get_log(vibe_id)` 工具，让 AI 能主动拉取特定 Agent 的错误信息。
  </action>
  <verify>
    <automated>vibe status --json (确保包含 cwd 和最新状态摘要)</automated>
  </verify>
  <done>AI 现在能精准获知每个 Agent 到底在干什么，具备了更好的“全局视野”。</done>
</task>

</tasks>

<verification>
1. 在 VSCode 终端启动 Claude Desktop 并加载 vibe MCP。
2. 指令 AI：“帮我在侧边开个 WezTerm 跑 ping，然后告诉我它的状态。”
3. 验证：外部 WezTerm 弹出 -> 任务运行 -> Claude 返回任务 ID。
4. 指令 AI：“检查环境。” -> 验证 AI 是否能正确识别 VSCode 并给出 fallback 建议。
</verification>

<output>
完成后创建 .planning/phases/07-ai-skill-integration/07-01-SUMMARY.md
</output>
