---
phase: 09-interactive-workflow-orchestration
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: [apps/vibe-cli/src/tui.rs, crates/vibe-core/src/ipc/server.rs]
autonomous: true
requirements: [SCO-01]

must_haves:
  truths:
    - "The TUI dashboard clearly displays which panes are 'WAITING' for approval."
    - "Pane status changes are broadcasted to the TUI in real-time."
  artifacts:
    - path: "apps/vibe-cli/src/tui.rs"
      provides: "Updated dashboard UI with approval labels"
  key_links:
    - from: "crates/vibe-core/src/ipc/server.rs"
      to: "apps/vibe-cli/src/tui.rs"
      via: "Broadcasted WorkerState with approval info"
---

<objective>
Update the TUI dashboard to provide visual feedback when an AI agent is waiting for human approval.

Purpose: To give the human operator immediate visibility into the state of the orchestration, specifically highlighting where their intervention is needed.
Output: Enhanced TUI status column and broadcast logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interactive-workflow-orchestration/09-CONTEXT.md
@.planning/phases/09-interactive-workflow-orchestration/09-RESEARCH.md
@.planning/phases/09-interactive-workflow-orchestration/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update State Broadcast Logic</name>
  <files>crates/vibe-core/src/ipc/server.rs</files>
  <read_first>
    - crates/vibe-core/src/ipc/server.rs (broadcast_states function)
    - crates/vibe-core/src/ipc/protocol.rs (WorkerState struct)
  </read_first>
  <action>
    - Update `broadcast_states` in `crates/vibe-core/src/ipc/server.rs` to include the new approval fields when mapping DB results to `WorkerState`.
    - Map `approval_status`, `plan_path`, and `rejection_reason` from the DB tuple.
  </action>
  <verify>
    <automated>cargo check -p vibe-core</automated>
  </verify>
  <acceptance_criteria>
    - `broadcast_states` correctly populates `WorkerState` with approval information from the database.
  </acceptance_criteria>
  <done>Master broadcasts approval status to subscribers.</done>
</task>

<task type="auto">
  <name>Task 2: Update TUI Dashboard UI</name>
  <files>apps/vibe-cli/src/tui.rs</files>
  <read_first>
    - apps/vibe-cli/src/tui.rs (table rendering logic)
  </read_first>
  <action>
    - Modify the pane table rendering in `apps/vibe-cli/src/tui.rs`.
    - If `approval_status` is "pending_approval", display "WAITING" or "REVIEW" in the Status column (use a distinct color like Yellow).
    - If "approved", you can optionally show a checkmark or keep "running".
    - If "rejected", show "REJECTED" (Red).
  </action>
  <verify>
    <automated>cargo check -p vibe-cli</automated>
  </verify>
  <acceptance_criteria>
    - The TUI displays "WAITING" in the status column for panes with pending approvals.
    - Status colors are consistent with the state (Yellow for pending, Red for rejected).
  </acceptance_criteria>
  <done>TUI dashboard enhanced with approval feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Master → TUI | State data is broadcasted to the TUI. The TUI only reads and displays this data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | TUI Broadcast | accept | Dashboard data is only available to local UDS subscribers. |
</threat_model>

<verification>
Run `vibe status` in one terminal, use `sqlite3` or a temporary tool to manually set a pane status to `pending_approval`, and verify the TUI updates instantly.
</verification>

<success_criteria>
1. TUI correctly reflects approval states.
2. Status column uses appropriate colors for better visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-workflow-orchestration/09-03-SUMMARY.md`
</output>
