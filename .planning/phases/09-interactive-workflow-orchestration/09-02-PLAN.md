---
phase: 09-interactive-workflow-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: [crates/vibe-core/src/env.rs, apps/vibe-cli/src/mcp.rs]
autonomous: true
requirements: [SCO-01]

must_haves:
  truths:
    - "AI agents can submit multi-step plans via the vibe_submit_plan tool."
    - "Submitted plans are saved as local Markdown files."
    - "AI agents can poll for approval status via the vibe_query_approval tool."
  artifacts:
    - path: "apps/vibe-cli/src/mcp.rs"
      provides: "New MCP tools: vibe_submit_plan, vibe_query_approval"
  key_links:
    - from: "apps/vibe-cli/src/mcp.rs"
      to: "crates/vibe-core/src/state/db.rs"
      via: "Updating approval status in SQLite"
    - from: "apps/vibe-cli/src/mcp.rs"
      to: "MasterServer"
      via: "Forwarding ApprovalRequest over UDS"
---

<objective>
Implement the MCP tools for submitting plans and querying approval status, including the persistence of plan content to local Markdown files.

Purpose: To enable AI agents to participate in the "Plan-Review-Execute" workflow by submitting their intent for human review.
Output: Two new MCP tools and a standard location for plan storage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interactive-workflow-orchestration/09-CONTEXT.md
@.planning/phases/09-interactive-workflow-orchestration/09-RESEARCH.md
@.planning/phases/09-interactive-workflow-orchestration/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan Storage Logic</name>
  <files>crates/vibe-core/src/env.rs</files>
  <read_first>
    - crates/vibe-core/src/env.rs (existing directory resolvers)
  </read_first>
  <action>
    - Add `resolve_plans_dir()` to `crates/vibe-core/src/env.rs` which returns `~/.vibe/plans/`.
    - Implement a helper (e.g., in a new `crates/vibe-core/src/state/plans.rs` or just in `env.rs`) to save a string to a timestamped Markdown file in the plans directory.
    - Path format: `~/.vibe/plans/plan_{vibe_id}_{timestamp}.md`.
  </action>
  <verify>
    <automated>ls ~/.vibe/plans/ (after manual or test run)</automated>
  </verify>
  <acceptance_criteria>
    - `resolve_plans_dir` correctly points to the plans directory.
    - Files are saved with `.md` extension.
    - Directory is created if it doesn't exist.
  </acceptance_criteria>
  <done>Plan storage directory and helper implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement vibe_submit_plan Tool</name>
  <files>apps/vibe-cli/src/mcp.rs</files>
  <read_first>
    - apps/vibe-cli/src/mcp.rs (existing tools list and handler)
    - crates/vibe-core/src/ipc/protocol.rs (Message enum)
  </read_first>
  <behavior>
    - The tool accepts `vibeId` and `plan` (markdown string).
    - It saves the plan to a file.
    - It updates the database status for the pane to `pending_approval` and records the `plan_path`.
    - It connects to the Master UDS and sends an `ApprovalRequest` message.
    - It returns a JSON object with `status: "pending"` and the file path.
  </behavior>
  <action>
    - Add `vibe_submit_plan` to the `tools/list` response in `handle_request`.
    - Update `call_tool` to handle `vibe_submit_plan`:
      1. Save plan text to `~/.vibe/plans/`.
      2. Call `db.update_approval_status(vibe_id, "pending_approval", Some(path), None)`.
      3. Connect to `resolve_socket_path()?` and send `Message::ApprovalRequest`.
      4. Return `{"status": "pending", "plan_path": path}`.
  </action>
  <verify>
    <automated>cargo test -p vibe-cli --lib mcp (requires mocking or integration env)</automated>
  </verify>
  <acceptance_criteria>
    - `vibe_submit_plan` is visible in `tools/list`.
    - Calling the tool results in a new file in `~/.vibe/plans/`.
    - Tool returns `status: "pending"`.
  </acceptance_criteria>
  <done>Submit plan tool functional.</done>
</task>

<task type="auto">
  <name>Task 3: Implement vibe_query_approval Tool</name>
  <files>apps/vibe-cli/src/mcp.rs</files>
  <read_first>
    - apps/vibe-cli/src/mcp.rs
    - crates/vibe-core/src/state/db.rs
  </read_first>
  <action>
    - Add `vibe_query_approval` to `tools/list`.
    - In `call_tool`, query the database for the pane's `approval_status`:
      - If "pending_approval" -> return `{"status": "pending"}`.
      - If "approved" -> return `{"status": "approved"}`.
      - If "rejected" -> return `{"status": "rejected", "reason": "..."}`.
  </action>
  <verify>
    <automated>cargo test -p vibe-cli --lib mcp</automated>
  </verify>
  <acceptance_criteria>
    - `vibe_query_approval` returns the correct status based on the database state.
    - Rejection reasons are correctly included in the response.
  </acceptance_criteria>
  <done>Query approval tool functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AI Client → MCP Tool | AI provides markdown content. MCP must ensure the file path is safe and content is stored only in the designated plans directory. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Spoofing | vibe_submit_plan | mitigate | Validate that the `vibeId` exists in the database before processing the plan. |
| T-09-04 | Information Disclosure | Plans Directory | accept | Plans are stored in the user's home directory with standard permissions. |
</threat_model>

<verification>
Start vibe master, call `vibe_submit_plan` via MCP-compatible client, verify file creation and DB status.
</verification>

<success_criteria>
1. `vibe_submit_plan` and `vibe_query_approval` work as specified.
2. Plans are correctly persisted to disk.
3. Master receives ApprovalRequest (verify via logs).
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-workflow-orchestration/09-02-SUMMARY.md`
</output>
