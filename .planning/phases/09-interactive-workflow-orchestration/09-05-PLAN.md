---
phase: 09-interactive-workflow-orchestration
plan: 05
type: execute
wave: 3
depends_on: ["09-02", "09-04"]
files_modified: [crates/vibe-core/src/ipc/server.rs]
autonomous: true
requirements: [SCO-01]

must_haves:
  truths:
    - "Master correctly routes ApprovalRequest from MCP to the target Worker."
    - "Master correctly processes ApprovalResult from Worker and updates the database."
    - "The end-to-end flow from Plan Submission to Approval to AI Polling works."
  artifacts:
    - path: "crates/vibe-core/src/ipc/server.rs"
      provides: "Approval message routing logic"
  key_links:
    - from: "crates/vibe-core/src/ipc/server.rs"
      to: "crates/vibe-core/src/state/db.rs"
      via: "Finalizing approval status in SQLite"
---

<objective>
Implement the routing and processing of approval messages in the Master server and perform final end-to-end verification of the workflow.

Purpose: To complete the "Plan-Review-Execute" loop by connecting the MCP tool requests to the Worker prompts and back to the AI's polling.
Output: Integrated master routing logic and verified workflow orchestration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interactive-workflow-orchestration/09-CONTEXT.md
@.planning/phases/09-interactive-workflow-orchestration/09-RESEARCH.md
@.planning/phases/09-interactive-workflow-orchestration/09-01-SUMMARY.md
@.planning/phases/09-interactive-workflow-orchestration/09-02-SUMMARY.md
@.planning/phases/09-interactive-workflow-orchestration/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Master Routing for Approval Messages</name>
  <files>crates/vibe-core/src/ipc/server.rs</files>
  <read_first>
    - crates/vibe-core/src/ipc/server.rs (handle_connection function)
  </read_first>
  <action>
    - Update `handle_connection` in `crates/vibe-core/src/ipc/server.rs`:
      - Handle `Message::ApprovalRequest { vibe_id, plan_path }`:
        1. Find the target worker's sender in `workers` map.
        2. Forward the message.
        3. Send `Message::Ack` back to the requester (MCP).
      - Handle `Message::ApprovalResult { vibe_id, approved, reason }`:
        1. Update the database status using `db.update_approval_status(vibe_id, if approved { "approved" } else { "rejected" }, None, reason)`.
        2. Trigger a state broadcast via `broadcast_states`.
        3. Send `Message::Ack` back to the worker.
  </action>
  <verify>
    <automated>cargo check -p vibe-core</automated>
  </verify>
  <acceptance_criteria>
    - `MasterServer` correctly routes `ApprovalRequest` to target worker.
    - `MasterServer` persists `ApprovalResult` to the database and broadcasts the change.
  </acceptance_criteria>
  <done>Master routing logic complete.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-End Verification</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <read_first>
    - apps/vibe-cli/src/mcp.rs
    - apps/vibe-cli/src/main.rs
  </read_first>
  <action>
    - Perform a manual end-to-end test of the entire flow:
      1. Start master: `vibe master`.
      2. Start worker: `vibe run --role worker`.
      3. Start MCP server: `vibe mcp`.
      4. Simulate AI:
         - Call `vibe_submit_plan` via MCP for the worker ID.
         - Verify worker prompts for approval.
      5. Simulate Human:
         - Approve the plan in the worker terminal.
      6. Simulate AI:
         - Call `vibe_query_approval` for the worker ID.
         - Verify it returns `{"status": "approved"}`.
    - Also test the rejection path with a reason.
  </action>
  <verify>
    <automated>cargo test -p vibe-cli (full integration suite if available)</automated>
  </verify>
  <acceptance_criteria>
    - Full "Plan -> Review -> Approval -> Poll" cycle completes successfully.
    - Rejection reasons are correctly communicated back to the AI.
  </acceptance_criteria>
  <done>Phase 9 features verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| MCP Client → Master | Requester asks master to route an approval request. |
| Worker Client → Master | Worker reports user's decision. Master must ensure the worker is authorized for the reported vibe_id. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Spoofing | ApprovalResult | mitigate | Master should only accept `ApprovalResult` for a `vibe_id` from the connection that registered that `vibe_id`. |
</threat_model>

<verification>
Perform the manual E2E test steps described in Task 2.
</verification>

<success_criteria>
1. Complex AI sequences can be governed by human approval.
2. The system remains responsive and consistent throughout the process.
3. TUI reflects all transitions accurately.
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-workflow-orchestration/09-05-SUMMARY.md`
</output>
