---
phase: 09-interactive-workflow-orchestration
plan: 04
type: execute
wave: 3
depends_on: ["09-01"]
files_modified: [crates/vibe-core/src/ipc/client.rs]
autonomous: false
requirements: [SCO-01]

must_haves:
  truths:
    - "A human-in-the-loop prompt appears in the worker's terminal when a plan is submitted."
    - "Users can approve, reject, or reject with a reason."
    - "The worker communicates the user's decision back to the Master."
  artifacts:
    - path: "crates/vibe-core/src/ipc/client.rs"
      provides: "Approval prompt implementation in WorkerClient"
  key_links:
    - from: "crates/vibe-core/src/ipc/client.rs"
      to: "MasterServer"
      via: "ApprovalResult message"
---

<objective>
Implement the interactive human-in-the-loop (HITL) logic in the Worker client to prompt users for approval of submitted plans.

Purpose: To ensure that complex AI actions are reviewed and confirmed by a human within the same terminal context where the AI is operating.
Output: An interactive CLI prompt using `dialoguer` that captures the human's decision.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interactive-workflow-orchestration/09-CONTEXT.md
@.planning/phases/09-interactive-workflow-orchestration/09-RESEARCH.md
@.planning/phases/09-interactive-workflow-orchestration/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handle ApprovalRequest in WorkerClient</name>
  <files>crates/vibe-core/src/ipc/client.rs</files>
  <read_first>
    - crates/vibe-core/src/ipc/client.rs (run_worker loop and handle_intent function)
    - crates/vibe-core/src/ipc/protocol.rs (Message enum)
  </read_first>
  <action>
    - In `WorkerClient::run_worker`'s message loop, add a case for `Message::ApprovalRequest { vibe_id, plan_path }`.
    - Implement a new private method `handle_approval_request(&self, plan_path: &str) -> (bool, Option<String>)`.
    - Use `println!` to show the user the location of the plan: `[VIBE] New plan submitted for review: {plan_path}`.
    - Use `dialoguer`'s `Select` or `Confirm` + `Input` to prompt:
      1. Choice: "Approve", "Reject", "Reject with Reason".
      2. If "Reject with Reason", prompt for text input.
    - Return a tuple containing the `approved` status and optional `reason`.
  </action>
  <verify>
    <automated>cargo check -p vibe-core --lib ipc::client</automated>
  </verify>
  <acceptance_criteria>
    - `WorkerClient` correctly routes `ApprovalRequest` to the prompt logic.
    - Prompt logic includes an option for a rejection reason (per D-04).
    - Plan path is clearly displayed to the user (per D-03).
  </acceptance_criteria>
  <done>Worker prompt logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ApprovalResult Propagation</name>
  <files>crates/vibe-core/src/ipc/client.rs</files>
  <read_first>
    - crates/vibe-core/src/ipc/client.rs (send_msg helper)
  </read_first>
  <action>
    - In `run_worker`, after `handle_approval_request` returns, send a `Message::ApprovalResult` back to the Master via the existing `framed` socket.
    - Ensure the message includes the `vibe_id`, the `approved` boolean, and the `reason` string.
  </action>
  <verify>
    <automated>cargo test -p vibe-core --lib ipc::client::tests</automated>
  </verify>
  <acceptance_criteria>
    - The worker correctly sends an `ApprovalResult` message with all required fields.
  </acceptance_criteria>
  <done>User decision is sent back to Master.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Human-in-the-loop prompt for plans.</what-built>
  <how-to-verify>
    1. Start a master: `vibe master`.
    2. Start a worker: `vibe run`.
    3. Manually send an `ApprovalRequest` to the master socket for that worker's ID (e.g. using a script or netcat).
    4. Confirm that the worker's terminal shows the plan path and prompts for action.
    5. Choose "Approve" and verify the terminal returns to listening state.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Human Operator → Worker | The human operator provides the decision. The worker trusts this input for hitl validation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | Stdin Prompt | mitigate | Ensure the worker loop is not processing other intents while the prompt is active to avoid input hijacking. |
</threat_model>

<verification>
Start `vibe master` and `vibe run`, simulate a plan submission, and confirm the `dialoguer` prompt works as expected.
</verification>

<success_criteria>
1. Worker prompt captures all decision paths correctly.
2. Resulting `ApprovalResult` message is correctly formed and sent.
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-workflow-orchestration/09-04-SUMMARY.md`
</output>
