---
phase: 09-interactive-workflow-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [crates/vibe-core/src/ipc/protocol.rs, crates/vibe-core/src/state/mod.rs, crates/vibe-core/src/state/db.rs]
autonomous: true
requirements: [SCO-01]

must_haves:
  truths:
    - "The IPC protocol supports ApprovalRequest and ApprovalResult messages."
    - "The database schema includes fields for tracking pane approval status."
    - "DbHandle provides methods to update and query approval state."
  artifacts:
    - path: "crates/vibe-core/src/ipc/protocol.rs"
      provides: "Approval message definitions"
    - path: "crates/vibe-core/src/state/mod.rs"
      provides: "Schema migration for approval fields"
  key_links:
    - from: "crates/vibe-core/src/state/db.rs"
      to: "crates/vibe-core/src/state/mod.rs"
      via: "Database query updates"
---

<objective>
Define the core IPC protocol messages and database schema updates required for the Interactive Workflow Orchestration.

Purpose: To provide the foundation for communication between MCP, Master, and Worker regarding task plans and human approvals.
Output: Updated protocol types, database migration, and state management methods.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interactive-workflow-orchestration/09-CONTEXT.md
@.planning/phases/09-interactive-workflow-orchestration/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update IPC Protocol</name>
  <files>crates/vibe-core/src/ipc/protocol.rs</files>
  <read_first>
    - crates/vibe-core/src/ipc/protocol.rs (existing Message enum)
  </read_first>
  <behavior>
    - Add ApprovalRequest variant to Message enum containing vibe_id and plan_path.
    - Add ApprovalResult variant to Message enum containing vibe_id, approved (bool), and optional reason (String).
    - Update WorkerState struct to include approval_status, plan_path, and rejection_reason.
    - Serialization/Deserialization tests must pass for new variants.
  </behavior>
  <action>
    - Modify the `Message` enum in `crates/vibe-core/src/ipc/protocol.rs`:
      ```rust
      pub enum Message {
          // ... existing
          ApprovalRequest { vibe_id: String, plan_path: String },
          ApprovalResult { vibe_id: String, approved: bool, reason: Option<String> },
      }
      ```
    - Update `WorkerState` struct:
      ```rust
      pub struct WorkerState {
          // ... existing
          pub approval_status: String, // "none", "pending", "approved", "rejected"
          pub plan_path: Option<String>,
          pub rejection_reason: Option<String>,
      }
      ```
    - Add unit tests in the `tests` module of `protocol.rs` covering serialization/deserialization of these new messages.
  </action>
  <verify>
    <automated>cargo test -p vibe-core --lib ipc::protocol::tests</automated>
  </verify>
  <acceptance_criteria>
    - `crates/vibe-core/src/ipc/protocol.rs` contains `ApprovalRequest` and `ApprovalResult` in `Message` enum.
    - `WorkerState` contains `approval_status`, `plan_path`, and `rejection_reason`.
    - Unit tests for these new variants exist and pass.
  </acceptance_criteria>
  <done>IPC protocol extended and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Database Schema Evolution</name>
  <files>crates/vibe-core/src/state/mod.rs</files>
  <read_first>
    - crates/vibe-core/src/state/mod.rs (existing migrations)
    - crates/vibe-core/src/state/schema.sql (original schema)
  </read_first>
  <action>
    - Add a new migration in `StateStore::migrations()` in `crates/vibe-core/src/state/mod.rs`:
      ```rust
      M::up("ALTER TABLE panes ADD COLUMN approval_status TEXT DEFAULT 'none';
             ALTER TABLE panes ADD COLUMN plan_path TEXT;
             ALTER TABLE panes ADD COLUMN rejection_reason TEXT;")
      ```
    - Update `StateStore::list_active_panes` to return the new fields.
    - Update `WorkerState` mapping in `crates/vibe-core/src/ipc/server.rs` if necessary to match the new fields (or handle it in the next task).
  </action>
  <verify>
    <automated>cargo test -p vibe-core --lib state::tests</automated>
  </verify>
  <acceptance_criteria>
    - `StateStore::migrations()` contains the new `ALTER TABLE` statements.
    - `StateStore::list_active_panes` returns the new columns from SQLite.
    - `cargo test -p vibe-core` passes, confirming migration logic works.
  </acceptance_criteria>
  <done>Database schema updated via migration.</done>
</task>

<task type="auto">
  <name>Task 3: Update DbActor and DbHandle</name>
  <files>crates/vibe-core/src/state/db.rs</files>
  <read_first>
    - crates/vibe-core/src/state/db.rs (existing handle methods)
    - crates/vibe-core/src/state/mod.rs (newly added migration fields)
  </read_first>
  <action>
    - Add `UpdateApprovalStatus` variant to `DbRequest` enum:
      ```rust
      UpdateApprovalStatus(String, String, Option<String>, Option<String>, oneshot::Sender<Result<()>>), // vibe_id, status, plan_path, reason
      ```
    - Implement `DbActor::handle_request` for `UpdateApprovalStatus`.
    - Add `update_approval_status` method to `DbHandle`.
    - Ensure `GetPanes` returns the new approval fields in its result tuple.
  </action>
  <verify>
    <automated>cargo test -p vibe-core --lib state::db::tests</automated>
  </verify>
  <acceptance_criteria>
    - `DbRequest` and `DbHandle` include methods for updating approval status.
    - `DbActor` correctly persists these updates to the `panes` table.
    - `GetPanes` (or equivalent list method) returns the new columns.
  </acceptance_criteria>
  <done>State management layer supports approval tracking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| IPC Client → Master | Workers report status and send approval results. Master must validate that the sender has permission to update the status of the specific vibe_id. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Database | mitigate | Use rusqlite_migration to ensure atomic schema changes. Use DbActor to serialize all DB access. |
| T-09-02 | Information Disclosure | protocol.rs | accept | Plan paths are sent over local UDS. Access is restricted to the local user. |
</threat_model>

<verification>
Check that `cargo test -p vibe-core` passes and that the new protocol variants are usable in the codebase.
</verification>

<success_criteria>
1. IPC protocol supports approval messages.
2. Database schema updated with approval fields.
3. State layer can store and retrieve approval information.
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-workflow-orchestration/09-01-SUMMARY.md`
</output>
