---
phase: 23
plan: 04
type: execute
wave: 4
depends_on: [23-03]
files_modified: [plugin/vibe/scripts/sync.js, scripts/test-conductor.js]
autonomous: true
requirements: [FLOW-05]
must_haves:
  truths:
    - "Sync command identifies 'running' tasks without active locks as 'interrupted'"
    - "Sync command releases orphaned locks from crashed processes"
    - "E2E test suite covers the full planning-execution-review cycle"
  artifacts:
    - path: "plugin/vibe/scripts/sync.js"
      provides: "Recovery and state synchronization logic"
    - path: "scripts/test-conductor.js"
      provides: "E2E integration test suite"
  key_links:
    - from: "plugin/vibe/scripts/sync.js"
      to: ".vibe/locks/"
      via: "stale lock detection"
---

<objective>
实现中断恢复与同步逻辑，并通过全自动集成测试验证 Phase 23 的完整闭环。

Purpose: 提高系统的鲁棒性，确保在 Agent 或进程崩溃后能安全恢复工作流。
Output: `sync.js` 脚本和 `test-conductor.js` 测试套件。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/23-CONTEXT.md
@.planning/phases/23-RESEARCH.md
@.planning/phases/23-VALIDATION.md
@.planning/phases/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sync.js for interruption recovery</name>
  <files>plugin/vibe/scripts/sync.js</files>
  <action>
    - 实现 `plugin/vibe/scripts/sync.js` (per D-04)：
        - 扫描 `.vibe/tasks/*.json` 中状态为 `running` 的任务。
        - 检查 `.vibe/locks/` 下是否有对应的锁文件。
        - (可选但推荐) 检查锁文件中记录的 PID 是否仍在运行。
        - 如果任务标记为 `running` 但没有有效锁/进程：
            - 将状态更新为 `interrupted`。
            - 打印警告并列出受影响的任务。
        - 扫描并清除不属于任何 `running` 任务的孤儿锁文件。
  </action>
  <verify>
    <automated>
      # 模拟中断场景
      echo '{"id":"T1", "status":"running"}' > .vibe/tasks/T1.json
      # 不创建锁文件
      node plugin/vibe/scripts/sync.js
      grep "interrupted" .vibe/tasks/T1.json
    </automated>
  </verify>
  <done>sync.js 能够准确识别中断任务并清理环境</done>
</task>

<task type="auto">
  <name>Task 2: Implement E2E integration test suite</name>
  <files>scripts/test-conductor.js</files>
  <action>
    - 按照 `23-VALIDATION.md` 编写 `scripts/test-conductor.js` (Pure Node.js)：
        - **T1**: 模拟单任务快乐路径。
        - **T2**: 模拟依赖链阻塞与解锁。
        - **T3**: 模拟 Review 失败后的 `fix-needed` 循环与重置。
        - **T4**: 模拟达到 3 次修复上限后的 `failed`。
        - **T5**: 模拟进程崩溃后的 `sync` 恢复过程。
    - 确保测试脚本能在无真实模型连接的情况下运行（通过 Mock Agent 输出）。
  </action>
  <verify>
    <automated>node scripts/test-conductor.js</automated>
  </verify>
  <done>完整的 E2E 测试套件覆盖了 Phase 23 的所有关键功能</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Scripts → OS Process Table | sync.js checks PID status (if implemented) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Tampering | .vibe/locks | mitigate | sync.js uses atomic file operations and validates lock ownership before deletion |
</threat_model>

<verification>
1. 运行 `node scripts/test-conductor.js`。
2. 验证所有测试用例 (T1-T5) 均通过。
3. 手动模拟一次 `vibe run-task` 中途强杀进程，验证 `vibe status` (通过 sync) 能报告中断。
</verification>

<success_criteria>
- `sync.js` 能够识别并修复不一致的运行状态。
- 自动集成测试覆盖率为 100% (关键路径)。
- 验证策略符合 `23-VALIDATION.md`。
</success_criteria>

<output>
After completion, create `.planning/phases/23-多模型执行与审查闭环/23-04-SUMMARY.md`
</output>
