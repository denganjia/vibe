---
phase: 23
plan: 04
type: execute
wave: 3
depends_on: [23-02, 23-03]
files_modified: [plugin/vibe/scripts/sync.js, plugin/vibe/scripts/status.js]
autonomous: true
requirements: [FLOW-05]
must_haves:
  truths:
    - "System identifies and recovers from 'phantom' tasks (processes that crashed without cleaning up)."
    - "User is notified of interrupted tasks and can choose to resume or reset them."
    - "A single command allows resetting all interrupted tasks to a clean state for re-execution."
  artifacts:
    - path: "plugin/vibe/scripts/sync.js"
      provides: "Workspace state synchronization"
    - path: "plugin/vibe/scripts/status.js"
      provides: "Health warning display"
  key_links:
    - from: "plugin/vibe/scripts/sync.js"
      to: ".vibe/locks/"
      via: "fs.readdirSync and fs.existsSync"
---

<objective>
Implement recovery and synchronization logic to handle interrupted Agent processes or stale workspace state. This ensures that the Conductor can resume work after a crash or manual termination.

Purpose: To maintain workflow continuity and handle real-world interruptions.
Output: Sync script and updated status reporting for interrupted tasks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-CONTEXT.md
@.planning/phases/23-RESEARCH.md
@plugin/vibe/scripts/status.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Workspace Sync Script (sync.js)</name>
  <files>plugin/vibe/scripts/sync.js</files>
  <action>
    - Create `plugin/vibe/scripts/sync.js` as a pure Node.js script.
    - Logic:
        1. Scan `.vibe/tasks/*.json` for any task with `status === 'running'`.
        2. For each such task, check if a corresponding lock file exists in `.vibe/locks/`.
        3. If no lock exists, transition the task status to `interrupted`.
    - Reference D-04: Self-healing logic.
  </action>
  <verify>
    <automated>
      # Setup: Create running task with NO lock
      node plugin/vibe/scripts/task.js create '{"id": "t_int", "status": "running"}' > /dev/null
      node plugin/vibe/scripts/sync.js
      node plugin/vibe/scripts/status.js get t_int | grep "interrupted"
    </automated>
  </verify>
  <done>sync.js correctly identifies and marks interrupted tasks in the workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Sync and Warnings in status.js</name>
  <files>plugin/vibe/scripts/status.js</files>
  <action>
    - Update `plugin/vibe/scripts/status.js` to automatically call the sync logic or provide a `health` command.
    - Display a prominent "INTERRUPTED" warning if any tasks are in that state.
    - Provide a suggested command for recovery (e.g., `vibe sync --fix`).
    - Reference D-04: Conductor self-healing.
  </action>
  <verify>
    <automated>
      node plugin/vibe/scripts/task.js create '{"id": "t_warn", "status": "interrupted"}' > /dev/null
      node plugin/vibe/scripts/status.js | grep "INTERRUPTED"
    </automated>
  </verify>
  <done>status.js reports workspace health and identifies recovery needs.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Recovery Fix in sync.js</name>
  <files>plugin/vibe/scripts/sync.js</files>
  <action>
    - Add a `--fix` flag to `plugin/vibe/scripts/sync.js`.
    - Logic: When `--fix` is passed, transition all tasks with status `interrupted` back to `queued`.
    - This allows them to be picked up by the next `run-task` execution.
    - Ensure pure Node.js implementation.
  </action>
  <verify>
    <automated>
      node plugin/vibe/scripts/sync.js --fix
      node plugin/vibe/scripts/status.js get t_warn | grep "queued"
    </automated>
  </verify>
  <done>The system provides a clear path for resetting interrupted tasks to a clean state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System → sync.js | Relying on .vibe/locks which could be deleted by external processes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Tampering | .vibe/locks | accept | Workspace is local, user/other scripts might delete locks. |
| T-23-08 | Information Disclosure | logs/ | mitigate | Sync script should not expose log contents unnecessarily. |
</threat_model>

<verification>
- Manually simulate an interruption by creating a `running` task without a lock.
- Verify `sync.js` transitions it to `interrupted`.
- Verify `status.js` displays the warning.
- Verify `sync.js --fix` resets the status to `queued`.
</verification>

<success_criteria>
- Phantom `running` tasks are correctly identified.
- Interrupted tasks are clearly flagged in the UI/CLI.
- A functional recovery path exists via the `--fix` flag.
</success_criteria>

<output>
After completion, create `.planning/phases/23-多模型执行与审查闭环/23-04-SUMMARY.md`
</output>
