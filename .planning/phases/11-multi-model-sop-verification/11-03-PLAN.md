---
phase: 11-multi-model-sop-verification
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified: [skills/vibe-operator/SKILL.md]
autonomous: true
requirements: [SKL-02, SKL-03]
must_haves:
  truths:
    - "SKILL.md links to all new specialized SOPs."
    - "Multi-model collaboration logic is verified via a mock scenario."
  artifacts:
    - path: "skills/vibe-operator/SKILL.md"
      provides: "Updated skill entry point"
  key_links:
    - from: "skills/vibe-operator/SKILL.md"
      to: "skills/vibe-operator/sops/collaboration.md"
      via: "Markdown link"
---

<objective>
Integrate the new SOPs into the main skill definition and verify the collaboration logic via a dry-run.

Purpose: Finalize the multi-model SOP infrastructure and ensure it works in practice.
Output: Updated SKILL.md and verification summary.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-multi-model-sop-verification/11-CONTEXT.md
@.planning/phases/11-multi-model-sop-verification/11-02-SUMMARY.md
@skills/vibe-operator/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Link SOPs in SKILL.md</name>
  <files>skills/vibe-operator/SKILL.md</files>
  <action>
    - Update the 'Operating Protocols' section in SKILL.md.
    - Add specific links to:
      - [Collaboration SOP](./sops/collaboration.md)
      - [Verification SOP](./sops/verification.md)
      - [Recovery SOP](./sops/recovery.md)
    - Ensure descriptions briefly explain when to use each SOP.
  </action>
  <verify>
    <automated>grep -i "collaboration.md" skills/vibe-operator/SKILL.md && grep -i "verification.md" skills/vibe-operator/SKILL.md</automated>
  </verify>
  <done>SKILL.md contains links to all new SOP documents.</done>
</task>

<task type="auto">
  <name>Mock Dry-run Verification</name>
  <files></files>
  <action>
    - Simulate a multi-model scenario using vibe-cli commands (via shell).
    - Scenario:
      1. Conductor (Master) lists workers: `vibe list`.
      2. Worker reports a loop: `vibe report --status failed --message "[FAILED] Loop detected. Error repeat 2."`.
      3. Conductor detects the 3rd repeat (simulated) and injects a fix: `vibe inject --vibe-id worker-1 --command "ls -la"`.
      4. Evaluator performs logic audit (simulated).
    - Note: This is a protocol verification, ensuring the commands and state flow support the SOP.
  </action>
  <verify>
    <automated># Simulated dry-run steps that use actual vibe-cli commands
vibe list
# The following are examples of what would be executed by the models
# No actual code change here, just command verification
</automated>
  </verify>
  <done>The command sequences for collaboration and recovery are verified to be functional and consistent with the SOP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SKILL.md → User | Exposure of internal protocols |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Information Disclosure | SOPs | accept | SOPs are intended to be public/shared within the skill definition for AI consumption. |
</threat_model>

<verification>
Ensure all links in SKILL.md are correct and point to existing files.
</verification>

<success_criteria>
1. SKILL.md is the central entry point for all Phase 11 SOPs.
2. The collaboration and recovery workflows are demonstrated to be possible via CLI.
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-model-sop-verification/11-03-SUMMARY.md`
</output>
