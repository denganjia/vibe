---
phase: 08-production-infrastructure
plan: 01
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - crates/vibe-core/Cargo.toml
  - crates/vibe-core/src/state/mod.rs
  - Cargo.toml
autonomous: true
requirements: ["INF-01", "OPS-01", "OPS-02"]

must_haves:
  truths:
    - "Master automatically upgrades SQLite schema without data loss"
    - "GitHub Actions config exists for cross-platform binary builds"
    - "One-line install scripts (sh/ps1) are ready"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Release automation"
    - path: "scripts/install.sh"
      provides: "Unix installation"
    - path: "scripts/install.ps1"
      provides: "Windows installation"
---

<objective>
构建生产级的发布基建，确保数据库具备平滑演进能力，并提供跨平台的一键安装体验。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 引入数据库迁移机制</name>
  <files>crates/vibe-core/Cargo.toml, crates/vibe-core/src/state/mod.rs</files>
  <action>
    1. 在 `vibe-core` 中引入 `rusqlite_migration` 依赖。
    2. 重构 `StateStore`：不再直接运行 `schema.sql`，而是定义有序的 `Migrations`。
    3. 定义 M1（初始结构）和 M2（添加 role, summary, cwd 等字段）。
    4. 确保程序启动时自动运行 `up_to_latest()`。
  </action>
  <verify>
    <automated>cargo test (验证迁移后的数据库查询依然正常)</automated>
  </verify>
  <done>解决了 Schema 变更导致的删库痛点，支持数据平滑升级。</done>
</task>

<task type="auto">
  <name>Task 2: 配置 GitHub Actions 自动化发布</name>
  <files>.github/workflows/release.yml</files>
  <action>
    1. 创建 `.github/workflows/release.yml`。
    2. 配置构建矩阵：macos-latest (x86/arm), ubuntu-latest, windows-latest。
    3. 实现自动打包二进制为 `.tar.gz` 或 `.zip`。
    4. 配置在推送 Tag 时自动创建 GitHub Release 并上传附件。
  </action>
  <verify>
    <manual>检查 workflow 语法是否正确。</manual>
  </verify>
  <done>实现了“打标即发布”的工业化流程。</done>
</task>

<task type="auto">
  <name>Task 3: 编写一键安装脚本 (Unix & Windows)</name>
  <files>scripts/install.sh, scripts/install.ps1</files>
  <action>
    1. `install.sh`: 检测架构，从 GitHub 下载最新 Release，解压至 `~/.local/bin`，并尝试更新 Shell 的 PATH。
    2. `install.ps1`: Windows 下的下载、解压及用户级环境变量更新。
    3. 提供 `README.md` 更新，告知用户如何通过 `curl | bash` 安装。
  </action>
  <verify>
    <manual>在本地模拟运行脚本，检查二进制是否被正确放置且 PATH 是否生效。</manual>
  </verify>
  <done>用户现在可以通过一行命令快速装机。</done>
</task>

<task type="auto">
  <name>Task 4: Brew & Winget 适配准备</name>
  <files>docs/DISTRIBUTION.md</files>
  <action>
    1. 编写发布文档，记录如何更新 Homebrew Tap 公式。
    2. 记录 Winget Releaser 的基本配置流程。
  </action>
  <verify>
    <manual>确认文档内容准确。</manual>
  </verify>
  <done>为后续进入主流包管理器打下基础。</done>
</task>

</tasks>

<verification>
1. 运行 Master，确认旧数据库（如果存在）能被正确识别或迁移。
2. 检查 .github 文件夹，确保 CI 配置完整。
3. 模拟执行安装脚本，确认 PATH 变更逻辑。
</verification>

<output>
完成后创建 .planning/phases/08-production-infrastructure/08-01-SUMMARY.md
</output>
