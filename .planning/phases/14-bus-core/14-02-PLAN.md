---
phase: 14-bus-core
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: [
  "crates/vibe-core/src/ipc/client.rs",
  "apps/vibe-cli/src/main.rs"
]
autonomous: true
requirements: [BUS-04, BUS-05]
must_haves:
  truths:
    - "vibe signal <NAME> [PAYLOAD] sends a signal to the bus"
    - "vibe wait <NAME> blocks until the signal is received"
  artifacts:
    - path: "crates/vibe-core/src/ipc/client.rs"
      provides: "signal/wait helper methods"
    - path: "apps/vibe-cli/src/main.rs"
      provides: "signal/wait CLI subcommands"
---

<objective>
Expose the signal bus functionality to the user and agents via the Vibe CLI and client library.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-bus-core/14-CONTEXT.md
@crates/vibe-core/src/ipc/client.rs
@apps/vibe-cli/src/main.rs
@.planning/phases/14-bus-core/14-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WorkerClient with Signal/Wait Helpers</name>
  <files>crates/vibe-core/src/ipc/client.rs</files>
  <action>
    Add public methods to `WorkerClient`:
    - `send_signal(name: String, payload: serde_json::Value) -> Result<()>`:
        Connect to server, send `Message::Signal`, wait for `Ack`.
    - `wait_for_signal(name: String, timeout_ms: u64) -> Result<serde_json::Value>`:
        Connect to server, send `Message::Wait`, wait for `Message::SignalFired`, return payload.
  </action>
  <verify>
    <automated>cargo check -p vibe-core</automated>
  </verify>
  <done>Client library supports signaling operations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement signal and wait CLI subcommands</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    Update `Commands` enum and `main()`:
    - Add `Signal { name: String, #[arg(trailing_var_arg = true)] payload: Vec<String> }`.
    - Add `Wait { name: String, #[arg(long)] timeout: Option<u64> }`.
    - Implement `Commands::Signal` logic:
        - Parse payload (if provided) as JSON or just use default.
        - Call `worker.send_signal()`.
    - Implement `Commands::Wait` logic:
        - Call `worker.wait_for_signal()`.
        - Print the received payload to stdout upon success.
    - Ensure both commands use the new project-isolated socket path.
  </action>
  <verify>
    <automated>cargo check -p vibe-cli</automated>
  </verify>
  <done>CLI supports vibe signal and vibe wait.</done>
</task>

<task type="auto">
  <name>Task 3: Integration Test of Signal Bus</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    Manual/Automated verification of the end-to-end flow:
    1. Start master: `vibe master --daemon` (if not running).
    2. In Terminal A: `vibe wait test_signal` (should block).
    3. In Terminal B: `vibe signal test_signal '{"status": "ok"}'`.
    4. Terminal A should unblock and print the JSON.
  </action>
  <verify>
    <automated># Run a small bash script to test this
vibe master --daemon && (sleep 1 && vibe signal test "hi" &) && vibe wait test
    </automated>
  </verify>
  <done>Signal bus is fully functional across processes.</done>
</task>

</tasks>

<success_criteria>
- `vibe signal` and `vibe wait` work as expected.
- Payloads are correctly transmitted.
- Blocking behavior is correct.
- Timeout behavior (if implemented) works.
</success_criteria>

<output>
After completion, create `.planning/phases/14-bus-core/14-02-SUMMARY.md`
</output>
