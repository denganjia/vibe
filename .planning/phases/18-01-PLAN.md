---
phase: 18-standardization-init
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [crates/vibe-core/src/adapter/mod.rs, crates/vibe-core/src/adapter/wezterm.rs, crates/vibe-core/src/adapter/tmux.rs, crates/vibe-core/src/state/mod.rs]
autonomous: true
requirements: [CLEAN-01, INIT-01]
must_haves:
  truths:
    - "TerminalAdapter can list all active physical pane IDs"
    - "ProjectConfig supports 'stacks' and implements deep merge for forward compatibility"
    - "StateStore can cleanup records whose physical IDs are no longer present"
  artifacts:
    - path: "crates/vibe-core/src/adapter/mod.rs"
      provides: "TerminalAdapter trait with list_all_physical_ids"
    - path: "crates/vibe-core/src/state/mod.rs"
      provides: "ProjectConfig with stacks field and deep merge logic"
  key_links:
    - from: "crates/vibe-core/src/state/mod.rs"
      to: "crates/vibe-core/src/adapter/mod.rs"
      via: "StateStore uses active physical IDs to prune stale records"
---

<objective>
升级 vibe-core 的核心模型和终端适配器，为标准化初始化和自动清理打下基础。

主要任务：
1. 在 TerminalAdapter 特性中增加物理 ID 列表查询。
2. 升级 ProjectConfig 以支持 Stacks（批量部署配置）。
3. 实现配置深度合并逻辑，确保在增加新字段时不会破坏用户的现有配置。
4. 在 StateStore 中实现静默清理逻辑。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-CONTEXT.md
@.planning/phases/18-standardization-init/18-RESEARCH.md
@crates/vibe-core/src/adapter/mod.rs
@crates/vibe-core/src/adapter/wezterm.rs
@crates/vibe-core/src/adapter/tmux.rs
@crates/vibe-core/src/state/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance TerminalAdapter with list_all_physical_ids</name>
  <files>crates/vibe-core/src/adapter/mod.rs, crates/vibe-core/src/adapter/wezterm.rs, crates/vibe-core/src/adapter/tmux.rs</files>
  <action>
    1. 在 `TerminalAdapter` trait 中增加 `fn list_all_physical_ids(&self) -> Result<Vec<String>>;`。
    2. 在 `WezTermAdapter` 中实现：调用 `wezterm cli list --format json`，解析输出并提取所有 `pane_id`。
    3. 在 `TmuxAdapter` 中实现：调用 `tmux list-panes -a -F "#{pane_id}"`。
  </action>
  <verify>
    <automated>cargo test --lib adapter::wezterm::tests adapter::tmux::tests</automated>
  </verify>
  <done>所有适配器均能正确返回物理 Pane ID 列表。</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade ProjectConfig and implement deep merge</name>
  <files>crates/vibe-core/src/state/mod.rs</files>
  <action>
    1. 修改 `ProjectConfig` 结构体：增加 `pub stacks: HashMap<String, Vec<String>>` 字段。
    2. 更新 `Default` 实现：默认提供 "default" stack (Conductor, Worker) 和 "full" stack (Conductor, Worker, Evaluator)。
    3. 在 `ConfigManager` 中实现 `deep_merge` 逻辑（参考 RESEARCH.md 中的模式）：
       - 将加载的 JSON 和默认配置合并。
       - 确保用户现有的 `roles` 得到保留，而新的 `stacks` 字段能被正确添加。
    4. 重构 `ConfigManager::load` 使用此合并逻辑。
  </action>
  <verify>
    <automated>cargo test --lib state::tests</automated>
  </verify>
  <done>ProjectConfig 支持 stacks 字段，且具备配置平滑升级能力。</done>
</task>

<task type="auto">
  <name>Task 3: Implement silent_cleanup in StateStore</name>
  <files>crates/vibe-core/src/state/mod.rs</files>
  <action>
    1. 在 `StateStore` 中增加 `pub fn cleanup_stale_panes(&self, active_physical_ids: &[String]) -> Result<()>`。
    2. 逻辑：遍历所有 `PaneRecord`，如果 `record.physical_id` 不在 `active_physical_ids` 列表中，则调用 `self.remove_pane(&record.vibe_id)`。
  </action>
  <verify>
    <automated>cargo test --lib state::tests</automated>
  </verify>
  <done>StateStore 具备根据物理状态清理残留记录的能力。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- CLI <-> Terminal Emulator (System command execution)
- CLI <-> File System (Config/State files)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | config.json | mitigate | Use atomic write for config files to prevent corruption. |
| T-18-02 | Information Disclosure | list_all_physical_ids | mitigate | Ensure terminal commands are executed with the current user's privileges only. |
</threat_model>

<verification>
运行 `cargo test` 确保核心逻辑无回归。
</verification>

<success_criteria>
1. `ProjectConfig` 已包含 `stacks` 字段且默认值正确。
2. 即使 `config.json` 缺少 `stacks` 字段，`ConfigManager::load` 也能正常返回包含默认 stacks 的对象。
3. `TerminalAdapter` 的实现能够获取当前终端环境中的所有 Pane。
</success_criteria>

<output>
After completion, create `.planning/phases/18-standardization-init/18-01-SUMMARY.md`
</output>
