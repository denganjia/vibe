---
phase: 03-state-persistence-ipc
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [crates/vibe-core/src/ipc/client.rs, apps/vibe-cli/src/main.rs]
autonomous: true
requirements: [STAT-01, STAT-02]

must_haves:
  truths:
    - "Worker can register with the master and send periodic heartbeats"
    - "The CLI can automatically start the master if it's not running"
    - "The vibe run command executes the specified process and streams its output"
  artifacts:
    - path: "crates/vibe-core/src/ipc/client.rs"
      provides: "Worker IPC client"
    - path: "apps/vibe-cli/src/main.rs"
      provides: "vibe run implementation"
---

<objective>
Implement the worker wrapper and integrate it into the CLI. This includes the Worker IPC client for registration/heartbeats and the `vibe run` subcommand with automatic master startup.

Purpose: To enable real-time tracking of tasks via a lightweight worker wrapper.
Output: Worker client and integrated `run` command.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/3-CONTEXT.md
@.planning/phases/03-RESEARCH.md
@.planning/phases/03-state-persistence-ipc/03-02-PLAN.md
@crates/vibe-core/src/ipc/protocol.rs
@crates/vibe-core/src/ipc/server.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Worker IPC Client</name>
  <files>crates/vibe-core/src/ipc/client.rs</files>
  <action>
    - Implement `WorkerClient` using `tokio::net::UnixStream`.
    - Handle registration with Master on startup (per D-07).
    - Implement 5-second periodic heartbeat using `tokio::time::interval` (per D-05).
    - Provide a way for the worker to report exit status to Master.
  </action>
  <verify>
    <automated>cargo test ipc::client::test_worker_heartbeat</automated>
  </verify>
  <done>Worker successfully registers and sends heartbeats that are received by the Master.</done>
</task>

<task type="auto">
  <name>Task 2: Implement vibe run subcommand and auto-start logic</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    - Add `run` subcommand: `vibe run <CMD> [ARGS]...`.
    - Logic: Check if Master UDS exists; if not, call `spawn_daemon()` from `vibe-core` (per D-01).
    - Spawn child process (CMD + ARGS) and wrap it in `WorkerClient`.
    - Stream stdout/stderr while sending heartbeats in the background.
    - Send final exit status message to Master before worker termination.
  </action>
  <verify>
    <automated>vibe run echo "Hello" && cargo test state::db::verify_echo_run</automated>
  </verify>
  <done>Running a command via `vibe run` results in a new pane entry in the database with periodic heartbeat updates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Command Args → OS | `vibe run` executes arbitrary commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | main.rs | accept | Child process inherits the current user's environment, which is standard for CLI tools. |
</threat_model>

<verification>
1. Run `vibe run sleep 15` and check the SQLite database for a new entry and updated heartbeats.
2. Verify that if the master is not running, it starts automatically when `vibe run` is called.
3. Verify that the child process output is still visible to the user.
</verification>

<success_criteria>
- Subcommand correctly spawns child processes and captures their output.
- Auto-start logic successfully launches the master in the background.
- Workers reliably send heartbeats every 5 seconds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-state-persistence-ipc/03-03-SUMMARY.md`
</output>
