---
phase: 03-state-persistence-ipc
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [crates/vibe-core/src/os/mod.rs, crates/vibe-core/src/os/unix.rs, crates/vibe-core/src/os/windows.rs, crates/vibe-core/src/ipc/server.rs]
autonomous: true
requirements: [STAT-02]

must_haves:
  truths:
    - "Master can start as a detached background process (daemon)"
    - "Master listens on a UDS socket at a standard cross-platform path"
    - "Master automatically exits after 10 minutes of idle time"
  artifacts:
    - path: "crates/vibe-core/src/os/mod.rs"
      provides: "Cross-platform daemonize function"
    - path: "crates/vibe-core/src/ipc/server.rs"
      provides: "UDS Master server"
---

<objective>
Implement the background Master server that coordinates state and listens for IPC messages. This includes cross-platform daemonization logic and the UDS listener with idle-timeout.

Purpose: To provide a persistent, background service for terminal state coordination.
Output: Daemonization utility and a functional UDS Master server.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/3-CONTEXT.md
@.planning/phases/03-RESEARCH.md
@.planning/phases/03-state-persistence-ipc/03-01-PLAN.md
@crates/vibe-core/src/ipc/protocol.rs
@crates/vibe-core/src/state/db.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement cross-platform daemonization</name>
  <files>crates/vibe-core/src/os/mod.rs, crates/vibe-core/src/os/unix.rs, crates/vibe-core/src/os/windows.rs</files>
  <action>
    - On Unix: Use `daemonize` crate to fork and detach (per Research 6.1).
    - On Windows: Use `DETACHED_PROCESS` and `CREATE_NO_WINDOW` flags via `CommandExt` (per Research 6.1).
    - Redirect stdout/stderr to `Stdio::null()` to avoid blocking the parent.
    - Expose a unified `spawn_daemon()` function in `vibe-core::os`.
  </action>
  <verify>
    <automated>cargo test os::test_daemon_spawn -- --ignored</automated>
  </verify>
  <done>A process can be spawned that persists after the parent exits.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Master UDS Server</name>
  <files>crates/vibe-core/src/ipc/server.rs</files>
  <action>
    - Implement `MasterServer` using `tokio::net::UnixListener`.
    - Handle socket path cleanup before `bind` (per D-01).
    - Implement NDJSON framing using `tokio_util::codec::LinesCodec`.
    - Process `Register` and `Heartbeat` messages by sending them to the `DbActor` (from Plan 01).
    - Implement 10-minute idle timeout: use a timer that resets on any message or active worker count > 0.
  </action>
  <verify>
    <automated>cargo test ipc::server::test_master_listener</automated>
  </verify>
  <done>Master listens for connections, updates the database via actor, and exits after the timeout when idle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System → UDS | The socket file is a point of entry. Unauthorized users on the same machine could send messages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | server.rs | mitigate | Limit maximum concurrent UDS connections. |
| T-03-04 | Information Disclosure | server.rs | mitigate | Ensure UDS socket file permissions are restricted to the current user (Unix). |
</threat_model>

<verification>
1. Manually start the master and verify it stays in the background.
2. Verify the `.sock` file is created in the expected config directory.
3. Check that the master process exits after the specified timeout if no workers connect.
</verification>

<success_criteria>
- Master server correctly binds to UDS on both Windows and Linux.
- Stale socket files are handled gracefully during startup.
- Master correctly delegates database updates to the actor.
</success_criteria>

<output>
After completion, create `.planning/phases/03-state-persistence-ipc/03-02-SUMMARY.md`
</output>
