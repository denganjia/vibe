---
phase: 03-state-persistence-ipc
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified: [tests/concurrency_test.rs]
autonomous: true
requirements: [STAT-01, STAT-02]

must_haves:
  truths:
    - "Multiple workers can register and send heartbeats concurrently"
    - "Master correctly handles multiple simultaneous UDS connections"
    - "Master correctly updates the database without locking conflicts"
  artifacts:
    - path: "tests/concurrency_test.rs"
      provides: "Validation suite for multi-worker scenarios"
---

<objective>
Verify that the system handles multiple concurrent workers and correctly persists their states in the SQLite database without contention or data loss.

Purpose: To ensure the robustness of the Master-Worker synchronization layer under load.
Output: Concurrency integration test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/3-CONTEXT.md
@.planning/phases/03-state-persistence-ipc/03-03-PLAN.md
@crates/vibe-core/src/ipc/server.rs
@crates/vibe-core/src/state/db.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement multi-worker concurrency test</name>
  <files>tests/concurrency_test.rs</files>
  <action>
    - Spawn a `vibe master` server.
    - Simulate 10-20 concurrent `vibe run` workers (using threads or async tasks).
    - Let them run for 10-15 seconds (to capture at least 2 heartbeats).
    - Verify all workers are registered in the SQLite database.
    - Verify all heartbeat timestamps are within a reasonable range (last 5-10s).
  </action>
  <verify>
    <automated>cargo test --test concurrency_test</automated>
  </verify>
  <done>Tests pass reliably and database consistently reflects the status of all concurrent workers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Master recovery validation</name>
  <files>tests/concurrency_test.rs</files>
  <action>
    - Start workers.
    - Kill the `vibe master` process (simulate crash).
    - Verify workers handle connection loss (they should retry connecting).
    - Start the `vibe master` again.
    - Verify workers successfully re-register and heartbeats resume.
  </action>
  <verify>
    <automated>cargo test --test concurrency_test::test_master_recovery</automated>
  </verify>
  <done>The system recovers from a Master crash, and heartbeats resume automatically without data loss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- None (test file only)
</threat_model>

<verification>
1. Run the new integration tests multiple times to check for race conditions.
2. Verify that no database locking errors are reported in the master logs.
</verification>

<success_criteria>
- Zero failures in the multi-worker concurrency test suite.
- Master correctly handles the reconnection of orphan workers after recovery.
- Database state remains consistent under high concurrency.
</success_criteria>

<output>
After completion, create `.planning/phases/03-state-persistence-ipc/03-04-SUMMARY.md`
</output>
