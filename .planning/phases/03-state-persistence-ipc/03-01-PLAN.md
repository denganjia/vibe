---
phase: 03-state-persistence-ipc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [crates/vibe-core/src/ipc/protocol.rs, crates/vibe-core/src/state/schema.rs, crates/vibe-core/src/state/db.rs]
autonomous: true
requirements: [STAT-01, STAT-02]

must_haves:
  truths:
    - "NDJSON message types are defined for Register and Heartbeat"
    - "SQLite schema is updated to track heartbeat and status"
    - "Database operations are serialized via an mpsc channel"
  artifacts:
    - path: "crates/vibe-core/src/ipc/protocol.rs"
      provides: "Message schemas"
    - path: "crates/vibe-core/src/state/schema.sql"
      provides: "Updated schema"
    - path: "crates/vibe-core/src/state/db.rs"
      provides: "Serialized DB actor"
  key_links:
    - from: "crates/vibe-core/src/state/db.rs"
      to: "crates/vibe-core/src/state/schema.rs"
      via: "sql query execution"
---

<objective>
Define the messaging protocol (NDJSON) and the centralized database actor for state management. This foundation ensures all subsequent IPC work uses a consistent protocol and handles state persistence without SQLite lock contention.

Purpose: To establish a single source of truth (SSOT) for the terminal orchestration state.
Output: Protocol types, updated SQLite schema, and a thread-safe database actor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/3-CONTEXT.md
@.planning/phases/03-RESEARCH.md
@crates/vibe-core/src/state/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define NDJSON protocol and update schema</name>
  <files>crates/vibe-core/src/ipc/protocol.rs, crates/vibe-core/src/state/schema.sql, crates/vibe-core/src/state/mod.rs</files>
  <behavior>
    - Protocol: Register and Heartbeat types must serialize to NDJSON.
    - Schema: Add last_heartbeat_at (DATETIME), role (TEXT), status (TEXT), and pid (INTEGER) columns to panes table.
  </behavior>
  <action>
    - Create `crates/vibe-core/src/ipc/protocol.rs` with `Message` enum (Register, Heartbeat, Ack).
    - Use `serde_json` for serialization (per D-04).
    - Update `crates/vibe-core/src/state/schema.sql` (per Research 3.1).
    - Expose the new protocol module in `crates/vibe-core/src/lib.rs` (if not already there) or `mod.rs`.
  </action>
  <verify>
    <automated>cargo test ipc::protocol</automated>
  </verify>
  <done>Protocol types serialize/deserialize correctly, and schema.sql contains new columns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement mpsc-based DB actor</name>
  <files>crates/vibe-core/src/state/db.rs</files>
  <behavior>
    - Only the actor task handles the SQLite connection (SSOT per D-10).
    - Accepts messages via mpsc to update pane status and heartbeats (per D-09).
  </behavior>
  <action>
    - Implement `DbActor` that owns a `rusqlite::Connection`.
    - Handle `DbRequest` enum: `UpdateHeartbeat(vibe_id)`, `RegisterPane(pane_info)`, `GetPanes`.
    - Wrap in a Handle that provides an async API to the rest of the system.
  </action>
  <verify>
    <automated>cargo test state::db::test_actor_concurrency</automated>
  </verify>
  <done>Database updates are successful through the async handle, and no "database is locked" errors occur under load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC Message → DB | Untrusted message content from workers could inject SQL if not handled via parameterized queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | protocol.rs | mitigate | Use serde for strict type validation of NDJSON messages. |
| T-03-02 | Elevation of Privilege | db.rs | mitigate | Ensure all SQL queries use prepared statements with parameters (rusqlite default). |
</threat_model>

<verification>
1. Run `cargo test` to ensure protocol and database logic pass.
2. Manually verify `schema.sql` reflects the research recommendations.
</verification>

<success_criteria>
- All message types defined in 3-CONTEXT.md are implemented.
- SQLite database can handle concurrent registration requests without locking issues.
- Heartbeat timestamps are correctly updated in the database.
</success_criteria>

<output>
After completion, create `.planning/phases/03-state-persistence-ipc/03-01-SUMMARY.md`
</output>
