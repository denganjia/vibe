---
phase: 15-autonomous-spawner
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified: [
  "apps/vibe-cli/src/main.rs",
  "crates/vibe-core/src/adapter/wezterm.rs",
  "crates/vibe-core/src/adapter/tmux.rs"
]
autonomous: true
requirements: [BUS-07, BUS-08, BUS-09]
must_haves:
  truths:
    - "Executing 'vibe spawn --role Worker' opens a new pane"
    - "The new pane contains the injected persona content"
    - "The agent command is automatically launched in the new pane"
  artifacts:
    - path: "apps/vibe-cli/src/main.rs"
      provides: "spawn command implementation"
  key_links:
    - from: "vibe spawn"
      to: "a-agent"
      via: "stdin injection"
      pattern: "adapter\\.inject_text\\(persona_content\\)"
---

<objective>
实现 `vibe spawn` 命令与增强的终端适配器。
主要目标：一键启动带有角色的 AI 代理，并实现 Persona 注入。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-autonomous-spawner/15-DISCOVERY.md
@apps/vibe-cli/src/main.rs
@crates/vibe-core/src/adapter/wezterm.rs
@crates/vibe-core/src/adapter/tmux.rs
</context>

<interfaces>
From crates/vibe-core/src/adapter/mod.rs:
```typescript
pub trait TerminalAdapter: Send + Sync {
    fn split(&self, direction: SplitDirection, size: Option<u32>, env_vars: std::collections::HashMap<String, String>) -> Result<VibeID>;
    fn send_keys(&self, target_id: &VibeID, keys: &str) -> Result<()>;
    fn inject_text(&self, target_id: &VibeID, text: &str) -> Result<()>;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 增强终端适配器对环境变量的支持</name>
  <files>crates/vibe-core/src/adapter/wezterm.rs, crates/vibe-core/src/adapter/tmux.rs</files>
  <action>
    - **WezTerm**: 在 `split` 方法中，如果 `env_vars` 不为空，将子命令包装在 `env VAR1=VAL1 ... bash` 中，确保环境变量能传导。
    - **Tmux**: 确保 `split` 方法中的 `-e` 参数能正确处理 `env_vars`。
  </action>
  <verify>
    <automated># 验证 split 后的新窗格中环境变量是否生效 (手动或脚本)</automated>
  </verify>
  <done>适配器能可靠地将 VIBE_MASTER_ID 传导至子窗格。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 vibe spawn 命令逻辑</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    - 在 `Commands` 枚举中添加 `Spawn { role: String, #[arg(long)] cmd: Option<String> }`。
    - 实现 `spawn` 逻辑：
        1. 使用 `vibe-core` 加载对应角色 Persona 及项目配置。
        2. 确定 `agent_command` (优先级：命令行 `--cmd` > 项目配置 > `a-agent`)。
        3. 获取当前 `master_pane_id`。
        4. 调用 `adapter.split` (传入 `VIBE_MASTER_ID` 等环境变量)。
        5. 调用 `adapter.inject_text` 注入角色 Persona。
        6. 调用 `adapter.send_keys` 启动 `agent_command`。
        7. 在 `StateStore` 中注册新窗格并设置其 Role。
  </action>
  <verify>
    <automated>cargo build -p vibe-cli</automated>
  </verify>
  <done>`vibe spawn` 能完成开窗、注入、启动的全流程。</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Spawner 功能端到端验证</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    1. 确保 `.vibe/roles/Worker.md` 存在。
    2. 运行 `vibe spawn --role Worker`。
    3. 观察新开启的窗格：
        - 验证是否有 Persona 文本出现在终端。
        - 验证 AI 代理程序是否已启动（如果是默认的 `a-agent` 且不存在，可能会报错，可先用 `echo` 测试）。
    4. 验证环境变量：在新窗格运行 `echo $VIBE_MASTER_ID` 检查是否正确。
  </action>
  <verify>
    <automated># 手动观察验证</automated>
  </verify>
  <done>Spawner 在真实终端环境下表现符合预期。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Shell execution | vibe spawn executes shell commands in new panes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Elevation of Privilege | Split Command Wrapper | mitigate | Use well-defined wrappers like `env` and `bash`; sanitize role names to avoid injection. |
</threat_model>

<success_criteria>
- `vibe spawn` 命令可用。
- 环境变量传导正常。
- 角色 Persona 成功注入子窗格 stdin。
</success_criteria>

<output>
After completion, create `.planning/phases/15-autonomous-spawner/15-02-SUMMARY.md`
</output>
