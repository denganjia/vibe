---
phase: 15-autonomous-spawner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "crates/vibe-core/src/state/mod.rs",
  "crates/vibe-core/src/lib.rs"
]
autonomous: true
requirements: [BUS-07, BUS-10]
must_haves:
  truths:
    - "Role templates are automatically initialized in .vibe/roles/ when vibe is first used"
    - "System can load persona content for a given role name"
    - "System can read/write project-level config.json"
  artifacts:
    - path: "crates/vibe-core/src/state/mod.rs"
      provides: "Role and Config management logic"
  key_links:
    - from: "vibe-core"
      to: ".vibe/roles/"
      via: "File system access"
      pattern: "vibe_dir\\.join\\(\"roles\"\\)"
---

<objective>
实现角色模板与项目配置管理系统。
主要目标：提供 `.vibe/roles/` 的自动化管理，以及读取项目级 `agent_command` 配置。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-autonomous-spawner/15-DISCOVERY.md
@crates/vibe-core/src/state/mod.rs
@crates/vibe-core/src/env.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 RoleManager 角色管理</name>
  <files>crates/vibe-core/src/state/mod.rs</files>
  <action>
    - 在 `state/mod.rs` 中添加 `RoleManager` 结构体（或在 `StateStore` 增加对应逻辑）。
    - 实现 `RoleManager::get_persona(role_name: &str) -> Result<String>`：
        - 从 `.vibe/roles/<role_name>.md` 读取文件内容。
    - 在 `ensure_project_vibe` 中添加默认角色初始化逻辑：
        - 如果 `roles/` 目录为空，自动创建 `Conductor.md`, `Worker.md`, `Evaluator.md`。
        - 提供这些角色的极简 Markdown 模板。
  </action>
  <verify>
    <automated># 编写简单测试验证角色文件被创建且内容可读取</automated>
  </verify>
  <done>角色系统能正确初始化并读取模板内容。</done>
</task>

<task type="auto">
  <name>Task 2: 实现项目级配置 (Config)</name>
  <files>crates/vibe-core/src/state/mod.rs</files>
  <action>
    - 定义 `ProjectConfig` 结构体：
        ```rust
        pub struct ProjectConfig {
            pub agent_command: String,
        }
        ```
    - 实现 `ConfigManager` 用于读写 `.vibe/config.json`。
    - 默认初始化 `agent_command` 为 `a-agent`。
  </action>
  <verify>
    <automated># 验证 config.json 在初始化时被创建且包含默认 agent_command</automated>
  </verify>
  <done>配置系统能正确读写 `.vibe/config.json`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System | The application reads/writes role templates and configs from .vibe directory |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | .vibe/roles/ | accept | User-local files; tampering affects current project only. |
| T-15-01-02 | Information Disclosure | .vibe/config.json | accept | Local project config; no sensitive secrets stored yet. |
</threat_model>

<success_criteria>
- `.vibe/roles/` 包含默认模板。
- `.vibe/config.json` 包含 `agent_command`。
- `vibe-core` 提供了相应的读取接口。
</success_criteria>

<output>
After completion, create `.planning/phases/15-autonomous-spawner/15-01-SUMMARY.md`
</output>
