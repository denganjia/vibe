---
phase: 12-workflow-templates-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [skills/vibe-operator/SKILL.md]
autonomous: true
requirements: [SKL-05]

must_haves:
  truths:
    - "Tool descriptions in SKILL.md are compressed for token efficiency."
    - "Prompt Variable Injection protocol is clearly documented with the $[VAR] syntax."
    - "A Quick Start 'Cheat Sheet' is present for rapid command sequence access."
  artifacts:
    - path: "skills/vibe-operator/SKILL.md"
      provides: "Optimized skill entry point and protocols"
---

<objective>
Optimize the Vibe-Operator skill definition for token efficiency and document the dynamic variable injection protocol.

Purpose: Reduce model context window usage and improve reliability in multi-step workflows.
Output: Updated and compressed SKILL.md.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-workflow-templates-optimization/12-CONTEXT.md
@.planning/phases/12-workflow-templates-optimization/12-RESEARCH.md
@skills/vibe-operator/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Surgically Update SKILL.md for Optimization and Protocols</name>
  <files>skills/vibe-operator/SKILL.md</files>
  <action>
    Update `skills/vibe-operator/SKILL.md` with the following changes:

    1. **Token Optimization**: Compress the 'Tool Reference' section. Use single-line compact descriptions.
       - Example: `- vibe_check: Verify terminal orchestration support.`
       - Example: `- vibe_list: List active agents, roles, and status.`
       - Example: `- vibe_split [vertical=bool]: Split current pane.`
       - Example: `- vibe_run [command, role]: Execute command in a new agent.`
    
    2. **Prompt Variable Injection Protocol**: Add a new section under 'Operating Protocols' explaining:
       - Syntax: `$[VARIABLE_NAME]`
       - Usage: AI must semantically inject values from history/docs during template parsing.
       - Fallback: Ask user if $[VAR] cannot be resolved.

    3. **Quick Start Cheat Sheet**: Add a 'Quick Start' section near the top of the file with common command sequences:
       - INIT: `vibe_check` -> `vibe_split` -> `vibe_run worker`
       - PLAN: `vibe_submit_plan` -> `vibe_query_approval`
       - EXEC: `vibe_inject [ID] "[CMD]"`
       - SYNC: `vibe_list` -> `vibe_focus [ID]`
  </action>
  <verify>
    <automated>grep "Prompt Variable Injection" skills/vibe-operator/SKILL.md && grep "Quick Start" skills/vibe-operator/SKILL.md</automated>
  </verify>
  <done>SKILL.md compressed and updated with new protocols and cheat sheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| $[VAR] Resolution | Model resolves variables based on context |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Info Disclosure | $[VAR] | mitigate | Protocol explicitly mentions resolving variables from *authorized* docs and history. |
</threat_model>

<success_criteria>
1. SKILL.md is significantly shorter in the Tool Reference section.
2. The `$[VARIABLE]` syntax is explained.
3. Quick Start commands are easily accessible.
</success_criteria>

<output>
After completion, create `.planning/phases/12-workflow-templates-optimization/12-02-SUMMARY.md`
</output>
