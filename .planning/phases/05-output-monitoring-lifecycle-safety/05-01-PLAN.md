---
phase: 05-output-monitoring-lifecycle-safety
plan: 01
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - crates/vibe-core/src/ipc/protocol.rs
  - crates/vibe-core/src/ipc/server.rs
  - crates/vibe-core/src/adapter/mod.rs
  - crates/vibe-core/src/adapter/wezterm.rs
  - crates/vibe-core/src/adapter/tmux.rs
  - crates/vibe-core/src/state/mod.rs
  - apps/vibe-cli/src/main.rs
autonomous: true
requirements: ["MON-01", "MON-03", "ORCH-02"]

must_haves:
  truths:
    - "Worker 输出被实时记录到剥离 ANSI 的本地日志文件中"
    - "Skill 可以通过 `vibe focus` 主动切换终端焦点"
    - "Skill 可以通过 `vibe report` 向 Master 提交结构化总结"
    - "Master 退出时，所有 Job Object 内的进程（含孙子进程）被强制清理"
  artifacts:
    - path: "crates/vibe-core/src/adapter/mod.rs"
      provides: "focus() trait method"
    - path: "apps/vibe-cli/src/main.rs"
      provides: "focus and report subcommands"
---

<objective>
为 AI Skill 提供主动反馈能力（汇报、聚焦）并确保日志采集的纯净性与进程安全性。

目的：实现“Skill 驱动反馈”的监控范式，增强系统稳定性与可观测性。
输出：日志采集系统、交互工具集（focus/report）、强化的进程清理机制。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 实现日志实时捕获与 ANSI 过滤</name>
  <files>crates/vibe-core/src/os/shell.rs, apps/vibe-cli/src/main.rs</files>
  <action>
    1. 在 `vibe-core` 中引入 ANSI 剥离逻辑（Regex 方案）。
    2. 更新 `vibe run` 逻辑：
       - 为每个任务在 `LocalData/vibe/logs/` 下创建 `{vibe_id}.log`。
       - 使用异步 Tee 模式，将 stdout/stderr 同时写入控制台和文件（写入文件前进行 ANSI Stripping）。
  </action>
  <verify>
    <automated>cargo test os::shell::test_ansi_stripping</automated>
  </verify>
  <done>日志采集系统就绪，日志文件内容纯净、无乱码。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 vibe focus 指令</name>
  <files>crates/vibe-core/src/adapter/mod.rs, crates/vibe-core/src/adapter/wezterm.rs, crates/vibe-core/src/adapter/tmux.rs, apps/vibe-cli/src/main.rs</files>
  <action>
    1. 在 `TerminalAdapter` trait 中新增 `focus(id: &str)` 方法。
    2. WezTerm 实现：调用 `wezterm cli activate-pane`。
    3. Tmux 实现：调用 `tmux select-pane`。
    4. 在 `vibe-cli` 中暴露 `focus <VIBE_ID>` 子命令。
  </action>
  <verify>
    <automated>vibe focus [ID] (人工观察焦点切换)</automated>
  </verify>
  <done>Skill 现在可以物理控制终端窗口的焦点了。</done>
</task>

<task type="auto">
  <name>Task 3: 实现 vibe report 指令与数据库持久化</name>
  <files>crates/vibe-core/src/ipc/protocol.rs, crates/vibe-core/src/ipc/server.rs, crates/vibe-core/src/state/mod.rs, apps/vibe-cli/src/main.rs</files>
  <action>
    1. 协议扩展：新增 `Report(ReportInfo)` 消息。
    2. 数据库扩展：在 `panes` 表中增加 `summary` 字段。
    3. Master 实现：收到 Report 后更新 SQLite 记录。
    4. CLI 实现：新增 `report --status <S> --msg <M>`。
  </action>
  <verify>
    <automated>vibe report --status success --msg "Done" && vibe list (查看 summary)</automated>
  </verify>
  <done>打通了从 Worker 到 Master 的逻辑汇报链路。</done>
</task>

</tasks>

<verification>
1. 运行一个长耗时任务：`vibe run ping google.com`。
2. 检查日志文件是否正在生成且无 ANSI 色彩码。
3. 在另一个窗格运行 `vibe focus` 确认切换。
4. 运行 `vibe report` 并在 `vibe list` 中看到结果。
</verification>

<output>
After completion, create .planning/phases/05-output-monitoring-lifecycle-safety/05-01-SUMMARY.md
</output>
