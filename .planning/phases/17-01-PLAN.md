---
phase: "17"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vibe-core/src/adapter/encoder.rs
  - crates/vibe-core/src/adapter/mod.rs
  - crates/vibe-core/src/adapter/wezterm.rs
  - crates/vibe-core/src/adapter/tmux.rs
autonomous: true
requirements:
  - INTERACT-1.3
must_haves:
  truths:
    - "Injection uses 5ms delay between chunks"
    - "Large text is split into 64-byte chunks during injection"
    - "WezTerm adapter uses --no-paste to avoid Bracketed Paste Mode issues"
  artifacts:
    - path: "crates/vibe-core/src/adapter/encoder.rs"
      provides: "TTY throttling logic"
  key_links:
    - from: "crates/vibe-core/src/adapter/mod.rs"
      to: "crates/vibe-core/src/adapter/encoder.rs"
      via: "TTYEncoder call in default inject_text"
---

<objective>
实现 TTY 编码器，为终端指令注入引入限流（Throttling）和精确的 `\r` 触发，解决 AI CLI 在接收长指令或回车符时的不一致行为。

Why: 解决 AI CLI 在 Raw Mode 下对快速注入或非标准回车符响应失败的问题。
Output: `TTYEncoder` 模块以及适配了该模块的 WezTerm/Tmux 适配器。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-CONTEXT.md
@.planning/phases/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 TTYEncoder 模块</name>
  <files>crates/vibe-core/src/adapter/encoder.rs, crates/vibe-core/src/adapter/mod.rs</files>
  <action>
    1. 创建 `crates/vibe-core/src/adapter/encoder.rs`。
    2. 实现 `TTYEncoder::throttle_inject<F>(text: &str, inject_fn: F)`，逻辑为：
       - 将文本拆分为 64 字节的块。
       - 遍历块，调用 `inject_fn` 并睡眠 5ms (`std::thread::sleep`)。
    3. 在 `crates/vibe-core/src/adapter/mod.rs` 中：
       - 引入 `pub mod encoder;`。
       - 更新 `TerminalAdapter` 接口，增加 `fn inject_raw(&self, target_id: &VibeID, text: &str) -> Result<()>;`。
       - 为 `inject_text` 提供默认实现，调用 `TTYEncoder::throttle_inject` 配合 `self.inject_raw`。
  </action>
  <verify>
    <automated>cargo test -p vibe-core --lib adapter::encoder</automated>
  </verify>
  <done>TTYEncoder 逻辑通过单元测试，TerminalAdapter 接口更新完成。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 WezTerm 和 Tmux 适配器</name>
  <files>crates/vibe-core/src/adapter/wezterm.rs, crates/vibe-core/src/adapter/tmux.rs</files>
  <action>
    1. 修改 `WezTermAdapter`:
       - 将原本 `inject_text` 的实现移动到 `inject_raw`。
       - 确保 `inject_raw` 调用 `wezterm cli send-text` 时带有 `--no-paste` 参数。
       - 移除显式的 `inject_text` 实现（使用 trait 默认实现）。
    2. 修改 `TmuxAdapter`:
       - 将原本 `inject_text` 的实现移动到 `inject_raw`。
       - 确保 `inject_raw` 调用 `tmux send-keys` 时带有 `-l` (literal) 参数。
       - 移除显式的 `inject_text` 实现（使用 trait 默认实现）。
       - 更新 `send_keys` 以调用 `self.inject_text(keys)` 后接 `self.inject_raw("\r")`。
  </action>
  <verify>
    <automated>cargo test -p vibe-core --lib adapter::wezterm && cargo test -p vibe-core --lib adapter::tmux</automated>
  </verify>
  <done>所有适配器均已支持限流注入，并统一使用 \r 触发。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Adapter -> Terminal CLI | 注入的文本作为 CLI 参数传递 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | TTYEncoder | mitigate | 在注入前不对文本做特殊转义，但依赖 Terminal CLI 的参数处理能力防止注入攻击 |
</threat_model>

<verification>
1. 验证 `vibe spawn` 注入长 Persona 时是否带有可见的延迟。
2. 验证注入指令后 AI CLI 是否能自动开始处理（无需手动按回车）。
</verification>

<success_criteria>
- 文本注入以 64 字节/5ms 的速率进行。
- WezTerm/Tmux 注入均使用 \r 作为结束符。
- 注入逻辑不再触发 Bracketed Paste Mode 的 UI 提示（WezTerm）。
</success_criteria>

<output>
After completion, create `.planning/phases/17-bi-directional-flow-reliability/17-01-SUMMARY.md`
</output>
