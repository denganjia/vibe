---
phase: "17"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vibe-core/src/ipc/bus.rs
  - crates/vibe-core/src/ipc/mod.rs
  - crates/vibe-core/src/env.rs
autonomous: true
requirements:
  - INTERACT-1.2
must_haves:
  truths:
    - "Signals are written as JSON files in .vibe/bus/"
    - "Signal files are written atomically via rename"
    - "Waiting for signals supports timeout and polling"
  artifacts:
    - path: "crates/vibe-core/src/ipc/bus.rs"
      provides: "File-based signal bus"
  key_links:
    - from: "crates/vibe-core/src/env.rs"
      to: "crates/vibe-core/src/ipc/bus.rs"
      via: "resolve_bus_dir path discovery"
---

<objective>
实现基于文件的信号总线（File-based Signal Bus），作为 Agent 间通信的可靠底层架构，彻底解决 stdin 劫持和 TTY 注入的不稳定性。

Why: 传统的 stdin/stdout 监控在 AI 代理繁忙时容易发生竞争或丢失，文件总线提供 100% 可靠的异步解耦。
Output: `FileBus` 模块及其原子化读写逻辑。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 FileBus 发送逻辑</name>
  <files>crates/vibe-core/src/ipc/bus.rs, crates/vibe-core/src/ipc/mod.rs, crates/vibe-core/src/env.rs</files>
  <action>
    1. 在 `crates/vibe-core/src/env.rs` 中增加 `resolve_bus_dir()`，指向 `.vibe/bus`。
    2. 在 `crates/vibe-core/src/ipc/mod.rs` 中引入 `pub mod bus;`。
    3. 创建 `crates/vibe-core/src/ipc/bus.rs`。
    4. 实现 `FileBus::send(signal_name: &str, payload: Value) -> Result<()>`:
       - 确保 `.vibe/bus` 目录存在。
       - 生成文件名: `{timestamp}-{uuid}.json`。
       - 原子写入: 先写临时文件，然后 `fs::rename`。
       - 负载使用 `crates/vibe-core/src/ipc/protocol.rs` 中的 `SignalInfo` 结构。
  </action>
  <verify>
    <automated>cargo test -p vibe-core --lib ipc::bus::tests::test_send</automated>
  </verify>
  <done>FileBus 能够稳定、原子地将信号写入磁盘。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 FileBus 接收（监听）逻辑</name>
  <files>crates/vibe-core/src/ipc/bus.rs</files>
  <action>
    1. 在 `FileBus` 中实现 `recv(signal_name: &str, timeout_secs: u64) -> Result<Value>`:
       - 采用异步轮询方式（每 100ms 扫描一次目录）。
       - 扫描 `.vibe/bus/` 下的所有文件，解析并匹配 `signal_name`。
       - 匹配成功后，读取 Payload 并立即删除该信号文件（消费模式）。
       - 如果超时，返回错误。
  </action>
  <verify>
    <automated>cargo test -p vibe-core --lib ipc::bus::tests::test_recv</automated>
  </verify>
  <done>FileBus 能够正确监听并消费特定信号。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| .vibe/bus/ directory | 任何有权访问该目录的进程都可以伪造信号 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Spoofing | FileBus | accept | 本地工具假设 .vibe 目录受到系统权限保护，不在此阶段引入加密签名 |
</threat_model>

<verification>
运行 `cargo test -p vibe-core --lib ipc::bus` 验证并发发送和接收的稳定性。
</verification>

<success_criteria>
- 信号文件以 JSON 格式存储。
- 写入过程是原子性的（无半写文件）。
- 接收端在读取后能够清理文件，防止信号重放。
</success_criteria>

<output>
After completion, create `.planning/phases/17-bi-directional-flow-reliability/17-02-SUMMARY.md`
</output>
