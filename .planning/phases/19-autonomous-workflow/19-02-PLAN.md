---
phase: "19"
plan: "02"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - "apps/vibe-cli/src/main.rs"
  - "apps/vibe-cli/src/tui.rs"
  - "DELIVERY.md"
autonomous: true
requirements:
  - "19.2"
must_haves:
  truths:
    - "TUI 逻辑已完全从 main.rs 拆分到 tui.rs"
    - "端到端重构测试期间未进行人工键盘输入"
    - "cargo test 验证通过"
    - "最终交付报告 DELIVERY.md 已生成"
  artifacts:
    - path: "apps/vibe-cli/src/tui.rs"
      provides: "TUI 模块实现"
    - path: "DELIVERY.md"
      provides: "E2E 压力测试最终交付报告"
  key_links:
    - from: "apps/vibe-cli/src/main.rs"
      to: "apps/vibe-cli/src/tui.rs"
      via: "module inclusion (mod tui;)"
---

<objective>
执行端到端（E2E）全自动化重构压力测试。

Purpose: 通过执行真实的重构任务（将 `main.rs` 的 TUI 逻辑拆分至 `tui.rs`），验证零人工干预的自治循环工作流，最终生成交付报告。
Output: 重构后的代码（`main.rs`, `tui.rs`）和一份综合性的 `DELIVERY.md` 报告。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-CONTEXT.md
@apps/vibe-cli/src/main.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: 启动全自动重构任务</name>
  <files>apps/vibe-cli/src/main.rs, apps/vibe-cli/src/tui.rs</files>
  <action>
    通过 Conductor 派生 Worker，遵循新的 SOP 对目标文件进行 Intent Locking，随后将 TUI 逻辑从 `apps/vibe-cli/src/main.rs` 完全拆分至新文件 `apps/vibe-cli/src/tui.rs`。Worker 必须运行 `cargo test` 进行验证，并在最多 3 次失败重试内解决编译问题，测试通过后通过 File-based Bus 发送 SUCCESS 信号。
  </action>
  <verify>
    <automated>cargo check -p vibe-cli</automated>
  </verify>
  <done>TUI 逻辑成功转移至 tui.rs，项目无编译与测试错误。</done>
</task>

<task type="auto">
  <name>Task 2: 生成最终交付报告 DELIVERY.md</name>
  <files>DELIVERY.md</files>
  <action>
    Conductor 汇总从 Worker 获取的所有 File-based Bus 信号结果，并将其与初始 REQUIREMENTS 对齐，自动生成名为 `DELIVERY.md` 的最终交付报告。
  </action>
  <verify>
    <automated>test -f DELIVERY.md</automated>
  </verify>
  <done>已成功生成 DELIVERY.md 包含完整的重构测试总结。</done>
</task>
</tasks>
