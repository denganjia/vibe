---
phase: "19"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "skills/vibe-operator/SKILL.md"
  - "skills/vibe-operator/references/collaboration.md"
  - ".vibe/roles/Conductor.md"
  - ".vibe/roles/Worker.md"
autonomous: true
requirements:
  - "19.1"
must_haves:
  truths:
    - "Vibe-Operator has an 'Analyze-Declare-Execute-Verify' SOP"
    - "Workers declare target files via 'vibe report' before modification"
    - "Workers retry up to 3 times on test failures before signaling BLOCKED"
    - "Conductor uses Intelligence-First logic to manage workflows"
  artifacts:
    - path: "skills/vibe-operator/SKILL.md"
      provides: "Updated SOP rules"
    - path: ".vibe/roles/Conductor.md"
      provides: "Conductor Persona with Intelligence-First"
  key_links:
    - from: ".vibe/roles/Conductor.md"
      to: ".vibe/bus"
      via: "File-based Bus monitoring"
---

<objective>
更新 SOP 与角色模板以支持全自治工作流。

Purpose: 为 Agent 引入 "Analyze-Declare-Execute-Verify" 循环、Intent Locking 机制（声明目标文件）、3 次 Fix 重试规则，并使 Persona 遵循 Intelligence-First 逻辑与 File-based Bus 信号标准。
Output: 更新的 `vibe-operator` 技能资产和 `Conductor`/`Worker` 角色模板。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-CONTEXT.md
@skills/vibe-operator/SKILL.md
@.vibe/roles/Conductor.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 更新 vibe-operator SOP 资产</name>
  <files>skills/vibe-operator/SKILL.md, skills/vibe-operator/references/collaboration.md</files>
  <action>
    修改 `SKILL.md` 和 `collaboration.md` 以引入 "Analyze-Declare-Execute-Verify" 自治循环。明确 Intent Locking（Worker 在修改前必须通过 `vibe report` 声明目标文件以防竞争）以及 Verification Gate（必须运行如 `cargo test` 的本地测试，在发送 BLOCKED 信号给 Conductor 前最多支持 3 次自动 Fix 重试）。
  </action>
  <verify>
    <automated>grep -q "Analyze-Declare-Execute-Verify" skills/vibe-operator/SKILL.md</automated>
  </verify>
  <done>SOP 文件成功包含新的意图锁定与重试规则。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 Conductor 与 Worker Persona 模板</name>
  <files>.vibe/roles/Conductor.md, .vibe/roles/Worker.md</files>
  <action>
    修改 `.vibe/roles/Conductor.md` 和 `.vibe/roles/Worker.md` 以遵循 Intelligence-First 决策逻辑和 File-based Bus 信号标准。确保 Conductor 能够监控 `.vibe/bus/` 信号并使用 `vibe inject`/`vibe spawn` 进行决策路由，而 Worker 需严格遵守 Intent Locking 与 3 次测试重试规则。
  </action>
  <verify>
    <automated>grep -q "Intelligence-First" .vibe/roles/Conductor.md</automated>
  </verify>
  <done>角色模板已反映新的决策逻辑和信号标准。</done>
</task>
</tasks>
