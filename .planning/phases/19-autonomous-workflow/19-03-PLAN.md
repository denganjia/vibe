---
phase: "19"
plan: "03"
type: execute
wave: 3
depends_on:
  - "19-02"
files_modified:
  - "Cargo.toml"
  - "apps/vibe-cli/Cargo.toml"
  - "crates/vibe-core/Cargo.toml"
autonomous: true
requirements:
  - "19.3"
must_haves:
  truths:
    - "所有 Cargo crate 版本更新至 5.0.0"
    - "Git v5.0 tag 成功创建"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace 版本定义"
  key_links: []
---

<objective>
发布 v5.0 正式版本。

Purpose: 在端到端自动化压力测试成功完成后，更新项目版本号并打上正式发布标签。
Output: 更新了版本号的 Cargo.toml 以及 v5.0 Git Tag。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 更新 Cargo 版本号</name>
  <files>Cargo.toml, apps/vibe-cli/Cargo.toml, crates/vibe-core/Cargo.toml</files>
  <action>
    将所有相关 `Cargo.toml` 文件中的版本号 `version` 字段更新为 `"5.0.0"`，并确保 workspace crate 内部依赖引用对应的版本。
  </action>
  <verify>
    <automated>grep -q 'version = "5.0.0"' Cargo.toml</automated>
  </verify>
  <done>所有目标文件中的版本号均变更为 5.0.0。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Git 发布标签</name>
  <files>.git/refs/tags/v5.0</files>
  <action>
    提交版本号更新的变更并打上名为 `v5.0` 的 Git Tag（如尚未打 Tag）。
  </action>
  <verify>
    <automated>git tag -l | grep -q "v5.0"</automated>
  </verify>
  <done>Git 仓库中成功存在 v5.0 标签。</done>
</task>
</tasks>
