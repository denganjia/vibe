---
phase: 21-vibe-agent
plan: 02
type: tdd
wave: 2
depends_on: [21-01]
files_modified:
  - plugin/vibe/scripts/init.js
  - plugin/vibe/scripts/init.test.js
autonomous: true
requirements: [VIBE-01, VIBE-04]
must_haves:
  truths:
    - "Initialization script creates required .vibe directories (agents, tasks, runs, locks, reviews, logs)"
    - "Initialization does not overwrite existing .vibe configurations without force flag"
    - "Initialization script is an independent JS runnable"
  artifacts:
    - path: "plugin/vibe/scripts/init.js"
      provides: "Non-destructive workspace initialization logic"
      contains: "fs.copyFileSync"
  key_links:
    - from: "plugin/vibe/scripts/init.js"
      to: "plugin/vibe/templates/.vibe"
      via: "template resolution"
      pattern: "templates.*\\.vibe"
---

<objective>
Implement the plugin initialization script to safely scaffold `.vibe` workspaces.

Purpose: Provides the standalone JS runtime logic for `vibe init` that non-destructively copies the pure JSON templates into the user's project, strictly adhering to the `vibe/commands/init.md` contract.
Output: A working `plugin/vibe/scripts/init.js` Node.js script.
</objective>

<task type="tdd">
  <name>Non-destructive initialization logic</name>
  <files>plugin/vibe/scripts/init.js, plugin/vibe/scripts/init.test.js</files>
  <action>
    **Behavior:**
    - Test 1: Given an empty directory, creates `.vibe/` with all subdirectories and copies all template files.
    - Test 2: Given an existing `.vibe/config.json`, does not overwrite it unless `--force` is passed.
    - Test 3: Even with `--force`, does not delete unrecognized `*.json` files in `.vibe/agents/`.

    **Implementation:**
    Implement `plugin/vibe/scripts/init.js` in vanilla Node.js (CommonJS, no external deps). It should accept `[target_dir]` and `--force` via `process.argv`. Read from `plugin/vibe/templates/.vibe`. Ensure directories (`agents`, `tasks`, `runs`, `locks`, `reviews`, `logs`) exist. Copy template files using `fs.existsSync` to skip existing files unless `--force` is enabled. Use the Node `assert` module for tests.
  </action>
  <verify>node plugin/vibe/scripts/init.test.js</verify>
  <done>All tests pass successfully.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System -> Script | Script reads from template dir and writes to project dir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Information Disclosure | `init.js` (fs ops) | Mitigate | Script must only copy from the known `plugin/vibe/templates` path, using `path.resolve` to prevent path traversal via `target_dir` CLI arguments. |
</threat_model>
