---
phase: 21-vibe-agent
plan: 03
type: execute
wave: 1
depends_on: ["21-01", "21-02"]
files_modified:
  - plugin/vibe/templates/.vibe/agents/planner.json
  - plugin/vibe/templates/.vibe/agents/executor.json
  - plugin/vibe/templates/.vibe/agents/reviewer.json
  - plugin/vibe/templates/.vibe/agents/release.json
  - plugin/vibe/templates/.vibe/config.json
  - plugin/vibe/scripts/init.test.js
autonomous: true
gap_closure: true
requirements: [VIBE-02, VIBE-03]
must_haves:
  truths:
    - "Agent 模板包含 prompt 和 reference 字段"
    - "config.json 模板包含 default_model 字段"
    - "初始化后的工作区保留这些字段"
  artifacts:
    - path: "plugin/vibe/templates/.vibe/agents/planner.json"
      provides: "Agent template with prompt/reference"
    - path: "plugin/vibe/templates/.vibe/config.json"
      provides: "Config template with default_model"
  key_links:
    - from: "plugin/vibe/scripts/init.test.js"
      to: "plugin/vibe/templates/.vibe"
      via: "content verification"
---

<objective>
修复 Phase 21 验证中发现的 Gap，完善 Agent 和 Config 模板字段，确保符合 Roadmap 和 VIBE-02/VIBE-03 要求。

Purpose: 为 Phase 22 提供完整的 Agent 引导和全局配置支持。
Output: 包含新增字段的模板文件和验证这些字段的测试用例。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-vibe-agent/21-VERIFICATION.md
@.planning/phases/21-vibe-agent/21-01-SUMMARY.md
@.planning/phases/21-vibe-agent/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 Agent 模板增加 prompt 和 reference 字段</name>
  <files>
    - plugin/vibe/templates/.vibe/agents/planner.json
    - plugin/vibe/templates/.vibe/agents/executor.json
    - plugin/vibe/templates/.vibe/agents/reviewer.json
    - plugin/vibe/templates/.vibe/agents/release.json
  </files>
  <action>
    对所有 Agent JSON 模板增加 "prompt" 和 "reference" 字段。
    - "prompt": 描述 Agent 职责的系统提示词引导。
    - "reference": 指向相关协议或文档的链接/路径。
    
    示例：
    planner.json -> prompt: "You are the Planner...", reference: "plugin/vibe/references/agent-contract.md"
    executor.json -> prompt: "You are the Executor...", reference: "plugin/vibe/references/task-contract.md"
    reviewer.json -> prompt: "You are the Reviewer...", reference: "plugin/vibe/references/review-protocol.md"
    release.json -> prompt: "You are the Release Agent...", reference: "plugin/vibe/references/release-summary.md" (如果存在)
  </action>
  <verify>
    grep -E "\"prompt\"|\"reference\"" plugin/vibe/templates/.vibe/agents/*.json
  </verify>
  <done>
    所有 Agent 模板均包含 prompt 和 reference 字段。
  </done>
</task>

<task type="auto">
  <name>Task 2: 为 config.json 增加 default_model 字段</name>
  <files>plugin/vibe/templates/.vibe/config.json</files>
  <action>
    在 config.json 模板根级别增加 "default_model": "claude" 字段。
  </action>
  <verify>
    grep "default_model" plugin/vibe/templates/.vibe/config.json
  </verify>
  <done>
    config.json 包含全局 default_model 配置。
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 init.test.js 以验证新字段</name>
  <files>plugin/vibe/scripts/init.test.js</files>
  <behavior>
    - 运行初始化后，读取生成的 config.json，断言包含 default_model。
    - 运行初始化后，读取生成的 planner.json，断言包含 prompt 和 reference。
  </behavior>
  <action>
    在 init.test.js 的 Test 1 中增加对 JSON 内容的校验。
    使用 JSON.parse 解析生成的配置文件并检查字段是否存在。
  </action>
  <verify>
    node plugin/vibe/scripts/init.test.js
  </verify>
  <done>
    测试用例能够验证新字段被正确拷贝。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| template -> workspace | 模板内容被拷贝到用户工作区，涉及配置注入。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering | Templates | mitigate | 模板仅在初始化时拷贝，后续由用户维护。使用 JSON 格式降低脚本执行风险。 |
</threat_model>

<verification>
运行 `node plugin/vibe/scripts/init.test.js` 确保所有逻辑通过，并手动检查 `plugin/vibe/templates/.vibe/` 下的模板文件。
</verification>

<success_criteria>
1. 所有 Agent JSON 模板包含 prompt 和 reference。
2. config.json 包含 default_model。
3. `init.test.js` 通过。
</success_criteria>

<output>
After completion, create .planning/phases/21-vibe-agent/21-03-SUMMARY.md
</output>
