---
phase: 18-standardization-init
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified: [apps/vibe-cli/Cargo.toml, apps/vibe-cli/src/main.rs]
autonomous: false
requirements: [INIT-01, CLEAN-01]
must_haves:
  truths:
    - "vibe init successfully scans environment and creates config.json"
    - "vibe list/spawn automatically removes stale physical ID records"
  artifacts:
    - path: "apps/vibe-cli/src/main.rs"
      provides: "Init command implementation and cleanup integration"
  key_links:
    - from: "apps/vibe-cli/src/main.rs"
      to: "crates/vibe-core/src/state/mod.rs"
      via: "CLI calls cleanup_stale_panes before major operations"
---

<objective>
实现 `vibe init` 交互式向导，并集成状态自动清理机制，提升用户体验和系统稳定性。

主要任务：
1. 在 `vibe-cli` 中实现 `init` 子命令，引导用户完成环境扫描和初始配置。
2. 在 `vibe spawn` 和 `vibe init` 的关键环节集成物理状态对比和清理逻辑。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-CONTEXT.md
@.planning/phases/18-standardization-init/18-RESEARCH.md
@.planning/phases/18-standardization-init/18-01-SUMMARY.md
@apps/vibe-cli/Cargo.toml
@apps/vibe-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and implement vibe init command</name>
  <files>apps/vibe-cli/Cargo.toml, apps/vibe-cli/src/main.rs</files>
  <action>
    1. 在 `apps/vibe-cli/Cargo.toml` 中添加 `dialoguer.workspace = true`。
    2. 在 `apps/vibe-cli/src/main.rs` 的 `Commands` 枚举中添加 `Init { #[arg(short, long)] force: bool }`。
    3. 实现 `handle_init` 逻辑：
       - 检测环境中的 AI CLI（claude, gemini, codex）。
       - 使用 `dialoguer` 提示用户选择默认 CLI。
       - 更新或创建 `.vibe/config.json`。
       - 调用 `ensure_project_vibe` 初始化角色模板（如果不强制，不要覆盖已有的模板）。
  </action>
  <verify>
    <automated>vibe init --help</automated>
  </verify>
  <done>vibe init 子命令已实现并能通过交互式引导完成配置。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>vibe init 交互式向导</what-built>
  <how-to-verify>
    1. 运行 `vibe init`。
    2. 验证是否能正确识别系统中的 AI CLI（如已安装）。
    3. 选择默认 CLI 并完成初始化。
    4. 检查 `.vibe/config.json` 是否生成并包含正确的配置。
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Integrate smart cleanup into CLI flows</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    1. 编写一个辅助函数 `perform_silent_cleanup(adapter: &dyn TerminalAdapter, store: &StateStore)`。
    2. 在 `vibe init` 的开始处调用此函数。
    3. 在 `vibe spawn` 的开始处调用此函数。
    4. 在 `vibe list` 的开始处调用此函数（作为一种便捷的手动清理方式）。
  </action>
  <verify>
    <automated># 模拟：手动修改 panes.json 增加一个不存在的物理 ID，然后运行 vibe list，确认记录被移除。</automated>
  </verify>
  <done>清理逻辑已集成到核心 CLI 流程中，能够自动维持状态库与物理终端的一致性。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- CLI <-> User (Interactive input)
- CLI <-> AI CLIs (Binary discovery)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Elevation of Privilege | which (binary discovery) | mitigate | Use the `which` crate to safely resolve paths and ensure only executable binaries are identified. |
</threat_model>

<verification>
验证 `vibe init` 和自动清理功能。
</verification>

<success_criteria>
1. `vibe init` 交互流程顺畅。
2. 运行 `vibe list` 后，过时的 Vibe 记录能被自动清除。
</success_criteria>

<output>
After completion, create `.planning/phases/18-standardization-init/18-02-SUMMARY.md`
</output>
