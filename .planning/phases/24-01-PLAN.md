---
phase: 24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [plugin/vibe/scripts/release-summary.js, plugin/vibe/commands/release-summary.md]
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04]
must_haves:
  truths:
    - "release-summary.js 可以正确提取 Git 日志并按 Conventional Commits 分类"
    - "脚本支持 --from 和 --to 参数自定义 Commit 区间"
    - "脚本生成 .vibe/RELEASE_DRAFT.md 文件，包含分类后的变更和文件统计"
    - "支持 --json 参数输出结构化数据"
  artifacts:
    - path: "plugin/vibe/scripts/release-summary.js"
      provides: "Release 总结核心逻辑"
    - path: "plugin/vibe/commands/release-summary.md"
      provides: "Plugin 命令合约"
  key_links:
    - from: "plugin/vibe/scripts/release-summary.js"
      to: "git log"
      via: "child_process.execSync"
---

<objective>
实现 Release 总结脚本及其插件集成，通过 Git 历史自动生成版本变更摘要。

Purpose: 提高发布效率，确保变更总结的确定性和准确性。
Output: `release-summary.js` 脚本与 `release-summary.md` 命令合约。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-CONTEXT.md
@.planning/phases/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 release-summary.js 脚本</name>
  <files>plugin/vibe/scripts/release-summary.js</files>
  <action>
    - 使用 Node.js `child_process.execSync` 调用 Git 命令。
    - 实现 `getGitLog(from, to)`：支持获取指定区间的 commit 历史，默认从 latest tag 到 HEAD。
    - 实现 `categorizeCommit(message)`：采用启发式算法识别 Conventional Commits 前缀（feat, fix, docs, test, refactor, chore）。
    - 实现 `getDiffStat(from, to)`：提取变更文件统计。
    - 支持 `--json` 输出。
    - 默认生成 `.vibe/RELEASE_DRAFT.md`，包含分类列表、作者和文件统计。
  </action>
  <verify>
    <automated>node plugin/vibe/scripts/release-summary.js --json</automated>
  </verify>
  <done>脚本可以运行并产出符合要求的 JSON 或 Markdown。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 release-summary.md 命令合约</name>
  <files>plugin/vibe/commands/release-summary.md</files>
  <action>
    - 创建 `plugin/vibe/commands/release-summary.md`。
    - 定义 `vibe release-summary` 命令，包括描述、用法示例及可选参数（--from, --to, --json）。
  </action>
  <verify>
    <automated>ls plugin/vibe/commands/release-summary.md</automated>
  </verify>
  <done>命令合约定义完成并符合项目标准。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Shell -> Git | 脚本执行系统命令获取 Git 历史 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | release-summary.js | mitigate | 脚本路径限制在 plugin/vibe/scripts/ 下 |
| T-24-02 | Information Disclosure | .vibe/RELEASE_DRAFT.md | mitigate | 确保不包含敏感环境变量，仅处理 Git log 信息 |
</threat_model>

<verification>
1. 运行 `node plugin/vibe/scripts/release-summary.js` 检查控制台输出和生成的 `.vibe/RELEASE_DRAFT.md`。
2. 运行 `node plugin/vibe/scripts/release-summary.js --json` 验证结构化输出。
</verification>

<success_criteria>
- 脚本能正确分类项目中的 commits。
- 生成的 Markdown 格式在 GitHub Release 中可直接预览。
</success_criteria>

<output>
After completion, create .planning/phases/24-release-summary/24-01-SUMMARY.md
</output>
