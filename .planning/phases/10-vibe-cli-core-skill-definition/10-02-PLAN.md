---
phase: 10-vibe-cli-core-skill-definition
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [skills/vibe-cli/sops/orchestration.md, skills/vibe-cli/sops/state.md, skills/vibe-cli/sops/approval.md]
autonomous: true
requirements: [SKL-01]
must_haves:
  truths:
    - "AI models have access to logical SOPs for pane management, state synchronization, and approvals."
    - "SOPs are abstract and conceptual, not hardcoded to environment-specific strings (D-06)."
  artifacts:
    - path: "skills/vibe-cli/sops/orchestration.md"
      provides: "Pane management logical flow"
    - path: "skills/vibe-cli/sops/state.md"
      provides: "IPC and state synchronization flow"
    - path: "skills/vibe-cli/sops/approval.md"
      provides: "Human-in-the-loop approval flow"
---

<objective>
Define the Standard Operating Procedures (SOPs) for `vibe-cli`.
These SOPs provide the "logical playbooks" that AI agents follow when performing core operations like environment setup, status tracking, and multi-step task submission.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-vibe-cli-core-skill-definition/10-CONTEXT.md
@.planning/phases/10-vibe-cli-core-skill-definition/10-RESEARCH.md
@skills/vibe-cli/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Create Orchestration SOP</name>
  <files>skills/vibe-cli/sops/orchestration.md</files>
  <action>
    Create `skills/vibe-cli/sops/orchestration.md` implementing D-06:
    1. Define the logical sequence for setting up a multi-pane environment:
       - Step 1: Detect environment (`vibe_check`).
       - Step 2: Determine orchestration capability.
       - Step 3: Split panes based on task needs (`vibe_split`).
       - Step 4: Focus and prepare workers (`vibe_run`).
    2. Explicitly avoid hardcoding terminal-specific commands (D-06). Use conceptual tool references.
  </action>
  <verify>
    <automated>grep "vibe_check" skills/vibe-cli/sops/orchestration.md && grep "vibe_split" skills/vibe-cli/sops/orchestration.md</automated>
  </verify>
  <done>Orchestration SOP created with logical flow.</done>
</task>

<task type="auto">
  <name>Create State and Approval SOPs</name>
  <files>skills/vibe-cli/sops/state.md, skills/vibe-cli/sops/approval.md</files>
  <action>
    1. Create `skills/vibe-cli/sops/state.md` defining how to maintain context:
       - Using `vibe_list` to poll status.
       - Reporting progress to avoid deadlocks.
       - Cross-pane status awareness.
    2. Create `skills/vibe-cli/sops/approval.md` defining the human-in-the-loop flow:
       - Submitting plans via `vibe_submit_plan`.
       - Polling for approval via `vibe_query_approval`.
       - Handling rejections or requested modifications.
    3. Include guidance on "AI Discretion" for triggering reviews (D-07).
  </action>
  <verify>
    <automated>ls skills/vibe-cli/sops/state.md skills/vibe-cli/sops/approval.md && grep "vibe_submit_plan" skills/vibe-cli/sops/approval.md</automated>
  </verify>
  <done>State synchronization and Approval SOPs created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AI -> User | Approval SOP defines the boundary where AI must wait for human intervention |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of Service | state.md | mitigate | Guidance to avoid tight polling loops with `vibe_list` |
| T-10-04 | Elevation of Privilege | approval.md | mitigate | Mandatory approval defined in SOP for high-risk operations |
</threat_model>

<success_criteria>
1. All 3 SOP files created in `skills/vibe-cli/sops/`.
2. Content follows the "Conceptual/Logical" guidance from D-06.
3. SOPs cover environment detection, pane management, state tracking, and the approval flow.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vibe-cli-core-skill-definition/10-02-SUMMARY.md`
</output>
