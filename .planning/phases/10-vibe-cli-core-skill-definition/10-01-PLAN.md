---
phase: 10-vibe-cli-core-skill-definition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills/vibe-cli/SKILL.yaml, skills/vibe-cli/SKILL.md, skills/vibe-cli/role.md]
autonomous: true
requirements: [SKL-01]
must_haves:
  truths:
    - "The `skills/vibe-cli/` directory exists with core definition files."
    - "AI models can identify the skill version and dependencies from `SKILL.yaml`."
    - "Master/Worker collaboration rules are defined in `role.md`."
  artifacts:
    - path: "skills/vibe-cli/SKILL.yaml"
      provides: "Metadata and dependency definitions"
    - path: "skills/vibe-cli/SKILL.md"
      provides: "Main instructional entry point for AI"
    - path: "skills/vibe-cli/role.md"
      provides: "Interactive initialization questions and role protocols"
  key_links:
    - from: "skills/vibe-cli/SKILL.md"
      to: "skills/vibe-cli/SKILL.yaml"
      via: "Reference for metadata"
    - from: "skills/vibe-cli/SKILL.md"
      to: "skills/vibe-cli/role.md"
      via: "Role and collaboration protocol reference"
---

<objective>
Initialize the `vibe-cli` skill package with its core metadata, instructional entry point, and collaboration protocols.
This provides the "identity" and "operating rules" for AI agents using the skill.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-vibe-cli-core-skill-definition/10-CONTEXT.md
@.planning/phases/10-vibe-cli-core-skill-definition/10-RESEARCH.md
@apps/vibe-cli/src/main.rs
@apps/vibe-cli/src/mcp.rs
</context>

<tasks>

<task type="auto">
  <name>Initialize Skill Package and Metadata</name>
  <files>skills/vibe-cli/SKILL.yaml, skills/vibe-cli/SKILL.md</files>
  <action>
    1. Create directory `skills/vibe-cli/`.
    2. Create `skills/vibe-cli/SKILL.yaml` containing:
       - name: vibe-cli
       - version: 0.1.0
       - description: Core skill for multi-agent orchestration and development using Vibe-CLI.
       - dependencies: [mcp:vibe-cli]
       - routing: default_mode: sdd, patterns: [{pattern: "sdd", template: "templates/sdd/"}]
    3. Create `skills/vibe-cli/SKILL.md` as the main entry point (per D-01).
       - Include a summary of vibe-cli's purpose.
       - Map core MCP tools (vibe_check, vibe_list, vibe_run, etc.) to their logical roles.
       - Link to `role.md`, `sops/`, and `templates/`.
  </action>
  <verify>
    <automated>ls -R skills/vibe-cli/ && grep "vibe-cli" skills/vibe-cli/SKILL.yaml</automated>
  </verify>
  <done>Core metadata and entry point files created and populated.</done>
</task>

<task type="auto">
  <name>Define Role Protocols and Initialization Questions</name>
  <files>skills/vibe-cli/role.md</files>
  <action>
    Create `skills/vibe-cli/role.md` implementing D-03, D-04, and D-05:
    1. Define "Master" and "Worker" roles with clear responsibilities:
       - Master: Orchestrates, assigns tasks, maintains global state, manages approvals.
       - Worker: Executes specific sub-tasks, reports status, requests clarification.
    2. Include the "Interactive Initialization" section with 5 core questions for the user (from RESEARCH.md):
       - Tech stack verification.
       - Terminal preference (Wezterm/Tmux).
       - Safety/Approval levels (Strict/Moderate/Relaxed).
       - UI focus preference.
       - Master autonomy level.
    3. Specify that this file is checked upon first use of the skill (D-04).
  </action>
  <verify>
    <automated>grep "Master" skills/vibe-cli/role.md && grep "Initialization Questions" skills/vibe-cli/role.md</automated>
  </verify>
  <done>`role.md` defined with roles and initialization protocol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AI -> Terminal | Skill definitions guide AI actions in the physical terminal |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | role.md | mitigate | role.md enforces strict hierarchical control and approval gates |
| T-10-02 | Info Disclosure | SKILL.md | mitigate | Instruct AI to mask sensitive env vars in logs |
</threat_model>

<success_criteria>
1. `skills/vibe-cli/` initialized with 3 files.
2. `SKILL.yaml` contains correct metadata.
3. `role.md` includes the required interactive question set and role definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vibe-cli-core-skill-definition/10-01-SUMMARY.md`
</output>
