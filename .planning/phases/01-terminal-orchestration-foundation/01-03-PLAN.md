---
phase: "01-terminal-orchestration-foundation"
plan: "03"
type: "execute"
wave: 3
depends_on: ["01-02"]
files_modified: ["src/state/mod.rs", "src/state/schema.sql", "src/main.rs", "tests/integration_tests.rs"]
autonomous: false
requirements: ["ORCH-01", "SAFE-02"]
user_setup: []

must_haves:
  truths:
    - "用户可以使用 `vibe split` 创建窗格，且窗格元数据已持久化到 SQLite。"
    - "用户运行 `vibe list` 能看到所有活跃的 Vibe 窗格及其角色。"
    - "运行 `vibe kill` 能够遍历数据库并关闭所有记录的物理窗格。"
  artifacts:
    - path: "src/state/mod.rs"
      provides: "SQLite persistence layer"
    - path: "src/main.rs"
      provides: "CLI entry points"
  key_links:
    - from: "src/main.rs"
      to: "src/state/mod.rs"
      via: "Database connection"
      pattern: "rusqlite::Connection"
---

<objective>
通过 SQLite 持久化层将逻辑 ID 与物理窗格关联，并最终实现用户可操作的 CLI 指令。

目的：完成编排指令的闭环，使得 Vibe 的操作具备持久性和可追溯性，并提供基础的用户交互接口。
输出：完整的 SQLite 状态管理模块，以及集成了切分、列表和强制销毁功能的 CLI 程序。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/1-RESEARCH.md
@.planning/phases/01-terminal-orchestration-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 SQLite 持久化层</name>
  <files>src/state/mod.rs, src/state/schema.sql</files>
  <action>
    1. 在 `src/state/schema.sql` 中定义 `panes` 表结构（参考 RESEARCH.md）。
    2. 在 `src/state/mod.rs` 中使用 `rusqlite` 实现数据库初始化逻辑。
    3. 实现 `save_pane(VibeID, PhysicalID, type)`、`get_pane(VibeID)`、`list_active_panes()` 和 `remove_pane(VibeID)` 等方法。
    4. 确保数据库文件存储在 Plan 01 计算出的 `state_dir` 中。
  </action>
  <verify>
    <automated>cargo test test_database_persistence</automated>
  </verify>
  <done>SQLite 状态管理模块实现完成，支持基本的 CRUD 操作。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心 CLI 指令</name>
  <files>src/main.rs</files>
  <action>
    1. 使用 `clap` 定义子命令：`split` (带参数 horizontal/vertical)、`list`、`kill`。
    2. 实现 `vibe split`：调用适配器切分窗格 -> 获取物理 ID -> 存入数据库。
    3. 实现 `vibe list`：从数据库读取并打印活跃窗格。
    4. 实现 `vibe kill`：从数据库获取所有窗格 -> 依次调用适配器的 `close` -> 清理数据库。
  </action>
  <verify>
    <automated>cargo run -- list</automated>
  </verify>
  <done>CLI 入口与业务逻辑打通，用户可通过指令管理窗格生命周期。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>完整的终端编排 MVP 功能，包含 WezTerm/Tmux 支持与持久化。</what-built>
  <how-to-verify>
    1. 在 WezTerm 内部运行：`vibe split --horizontal`，确认新窗格创建。
    2. 运行 `vibe list`，确认显示了新窗格。
    3. 运行 `vibe kill`，确认所有 Vibe 窗格被关闭。
    4. 检查 `.vibe/state.db` 是否存在并存储了数据。
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vibe-cli -> state.db | vibe-cli 向本地 SQLite 数据库写入状态数据。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | state.db | Accept | 假设本地用户拥有该目录权限。 |
| T-01-03-02 | Information Disclosure | Terminal output | Mitigate | CLI 输出不包含敏感环境变量。 |
</threat_model>

<verification>
运行 `cargo test` 运行集成测试。
人工在 WezTerm 和 Tmux 中验证 split/list/kill 流程。
</verification>

<success_criteria>
1. `vibe split` 能够在 WezTerm/Tmux 中创建真实的窗格。
2. 数据库中能够查看到逻辑 ID 与物理 ID 的对应关系。
3. `vibe kill` 能够彻底清理所有 Vibe 创建的窗格及数据库记录。
</success_criteria>

<output>
After completion, create .planning/phases/01-terminal-orchestration-foundation/01-03-SUMMARY.md
</output>
