---
phase: "01-terminal-orchestration-foundation"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["Cargo.toml", "src/main.rs", "src/adapter/mod.rs", "src/env.rs", "src/error.rs"]
autonomous: true
requirements: ["ORCH-01"]
user_setup: []

must_haves:
  truths:
    - "Rust 项目依赖项已正确配置，包含 tokio, rusqlite, serde_json 等。"
    - "能够识别当前是在 WezTerm 还是 Tmux 会话中。"
    - "TerminalAdapter trait 已定义，并包含 split, send_keys, close, get_metadata 方法。"
  artifacts:
    - path: "Cargo.toml"
      provides: "Project dependencies"
    - path: "src/adapter/mod.rs"
      provides: "TerminalAdapter abstraction"
    - path: "src/env.rs"
      provides: "Terminal environment detection"
  key_links:
    - from: "src/main.rs"
      to: "src/env.rs"
      via: "detect_current_terminal()"
      pattern: "detect_current_terminal"
---

<objective>
初始化 Rust 项目结构并定义终端编排的核心抽象层（TerminalAdapter trait）和环境探测逻辑。

目的：为跨平台和跨终端的编排奠定代码架构基础，确保后续适配器实现的统一性。
输出：完整的项目骨架、核心 trait 定义以及可靠的环境识别模块。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 Cargo.toml 与项目架构</name>
  <files>Cargo.toml, src/main.rs, src/adapter/mod.rs, src/error.rs</files>
  <action>
    1. 更新 `Cargo.toml`，添加以下依赖：`tokio` (full), `rusqlite` (bundled), `serde`, `serde_json`, `thiserror`, `anyhow`, `dirs`, `which`, `clap` (derive)。
    2. 创建 `src/error.rs` 定义自定义错误类型 `VibeError` (使用 thiserror)。
    3. 创建 `src/adapter/mod.rs` 定义 `TerminalAdapter` trait，根据 RESEARCH.md 包含：`split`, `send_keys`, `close`, `get_metadata`。
    4. 定义 `VibeID` (String) 和 `SplitDirection` (Horizontal/Vertical) 等辅助类型。
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>项目结构初始化完成，Trait 定义符合架构要求，代码可通过编译检查。</done>
</task>

<task type="auto">
  <name>Task 2: 实现环境探测与路径解析</name>
  <files>src/env.rs, src/main.rs</files>
  <action>
    1. 在 `src/env.rs` 中实现 `detect_current_terminal()`，优先检测 `WEZTERM_PANE` (WezTerm) 和 `TMUX` (Tmux) 环境变量。
    2. 实现 `resolve_config_dir()` 和 `resolve_state_dir()` (使用 `dirs` crate)，分别对应 Windows (%APPDATA%\vibe) 和 Unix (~/.config/vibe)。
    3. 在 `main.rs` 中调用检测逻辑，若未检测到支持的终端，执行 Fail Fast 策略并报错引导。
  </action>
  <verify>
    <automated>cargo run -- --help</automated>
  </verify>
  <done>能够准确识别 WezTerm/Tmux 环境，并正确计算跨平台配置路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> OS | 程序调用外部 CLI (wezterm/tmux) 时，环境变量的真实性跨越此边界。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | Env Detection | Mitigate | 仅使用系统标准环境变量，并在调用前验证其有效性。 |
| T-01-01-02 | Denial of Service | Path Resolution | Mitigate | 使用 `dirs` crate 标准化路径，处理无法创建目录的情况。 |
</threat_model>

<verification>
运行 `cargo check` 确保无编译错误。
在 WezTerm 和 Tmux 环境下分别运行 binary 确认环境识别输出。
</verification>

<success_criteria>
1. `TerminalAdapter` trait 完整定义。
2. WezTerm/Tmux 环境变量识别逻辑通过单元测试。
3. 跨平台路径计算逻辑符合 XDG/Windows 标准。
</success_criteria>

<output>
After completion, create .planning/phases/01-terminal-orchestration-foundation/01-01-SUMMARY.md
</output>
