---
phase: "01-terminal-orchestration-foundation"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01-01"]
files_modified: ["src/adapter/wezterm.rs", "src/adapter/tmux.rs", "src/os/mod.rs", "src/os/windows.rs"]
autonomous: true
requirements: ["ORCH-01", "SAFE-02"]
user_setup: []

must_haves:
  truths:
    - "WezTerm 适配器能够正确解析 `wezterm cli list --format json` 输出并识别当前窗格。"
    - "Tmux 适配器能够通过 `tmux list-panes -F` 获取窗格元数据。"
    - "在 Windows 环境下，Worker 进程能够加入 Job Object 以确保随主进程关闭而自动清理。"
  artifacts:
    - path: "src/adapter/wezterm.rs"
      provides: "WezTerm CLI implementation"
    - path: "src/adapter/tmux.rs"
      provides: "Tmux CLI implementation"
    - path: "src/os/windows.rs"
      provides: "Windows Job Objects support"
  key_links:
    - from: "src/adapter/wezterm.rs"
      to: "wezterm cli"
      via: "std::process::Command"
      pattern: "Command::new\\(\"wezterm\"\\)"
---

<objective>
实现 WezTerm 和 Tmux 的具体终端适配器，并集成 Windows 进程清理的安全机制。

目的：将抽象的 `TerminalAdapter` 转化为实际的编排能力，确保跨终端的一致性，并保障 Windows 环境下的进程回收安全。
输出：两个终端适配器的完整实现，以及针对 Windows 的 Job Objects 辅助模块。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/1-RESEARCH.md
@.planning/phases/01-terminal-orchestration-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 WezTerm 适配器</name>
  <files>src/adapter/wezterm.rs</files>
  <action>
    1. 实现 `WezTermAdapter` 结构体，封装 `wezterm cli` 调用。
    2. 实现 `split`：使用 `wezterm cli split-pane` 并解析返回的 `pane_id`。
    3. 实现 `get_metadata`：通过 `wezterm cli list --format json` 解析 JSON，准确映射物理 ID。
    4. 实现 `send_keys`：利用 `wezterm cli send-text`。
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>WezTerm 适配器核心方法实现完成，能够通过 WezTerm CLI 操控窗格。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 Tmux 适配器</name>
  <files>src/adapter/tmux.rs</files>
  <action>
    1. 实现 `TmuxAdapter` 结构体，封装 `tmux` 命令。
    2. 实现 `split`：使用 `tmux split-window` 并获取 `%id`。
    3. 实现 `get_metadata`：使用 `tmux list-panes -F` 结合 RESEARCH.md 中的 JSON 模板。
    4. 实现 `send_keys`：调用 `tmux send-keys`。
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Tmux 适配器核心方法实现完成，能够通过 Tmux 命令操控窗格。</done>
</task>

<task type="auto">
  <name>Task 3: 实现 Windows Job Objects 进程安全机制</name>
  <files>src/os/mod.rs, src/os/windows.rs</files>
  <action>
    1. 在 `src/os/windows.rs` 中利用 `windows-sys` crate 实现 Job Object。
    2. 设置 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` 标志位。
    3. 提供 `assign_to_job(child_handle)` 方法，确保所有 Worker 进程在 Vibe 退出时被自动回收。
    4. 在 `src/os/mod.rs` 中根据平台（cfg(windows)）导出该功能。
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Windows 环境下的进程树自动清理机制准备就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vibe-cli -> subprocess | vibe-cli 创建子进程执行 Worker 逻辑时存在隔离边界。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | wezterm/tmux CLI | Mitigate | 不在命令行参数中传递敏感信息，仅用于窗格控制。 |
| T-01-02-02 | Denial of Service | Zombie processes | Mitigate | 在 Windows 上使用 Job Objects (T3)，在 Unix 上后续通过进程组管理。 |
</threat_model>

<verification>
运行 `cargo check` 确保无编译错误。
在对应终端下编写简单的 mock 测试验证 CLI 输出解析。
</verification>

<success_criteria>
1. WezTerm 适配器通过 JSON 解析获得准确的物理 ID。
2. Tmux 适配器通过格式化字符串识别窗格。
3. Windows 代码能够在编译时正确分发并实现 Job Object。
</success_criteria>

<output>
After completion, create .planning/phases/01-terminal-orchestration-foundation/01-02-SUMMARY.md
</output>
