---
phase: "17"
plan: "03"
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - apps/vibe-cli/src/main.rs
  - scripts/verify_phase17.sh
autonomous: true
requirements:
  - INTERACT-1.1
  - INTERACT-1.2
  - AUTONOMY-3.1
must_haves:
  truths:
    - "vibe signal uses FileBus with TTY injection as fallback"
    - "vibe wait uses FileBus for reliable signal capture"
    - "Worker can complete a task and Master receives signal without user input"
  artifacts:
    - path: "scripts/verify_phase17.sh"
      provides: "E2E verification of autonomous workflow"
  key_links:
    - from: "apps/vibe-cli/src/main.rs"
      to: "crates/vibe-core/src/ipc/bus.rs"
      via: "FileBus call in Signal/Wait commands"
---

<objective>
更新 CLI 命令以适配新的文件总线，并建立回滚机制确保极端情况下的可用性。最终通过端到端脚本验证自治工作流的可靠性。

Why: 将底层架构的变更暴露给最终用户，并证明可靠性提升达到了 100% 自动化的目标。
Output: 更新后的 `vibe signal/wait` 命令以及 E2E 验证脚本。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-CONTEXT.md
@.planning/phases/17-01-SUMMARY.md
@.planning/phases/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 vibe signal 和 vibe wait 命令</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    1. 更新 `Commands::Signal` 处理逻辑:
       - 首先尝试使用 `FileBus::send` 发送信号。
       - 如果 `FileBus::send` 返回错误（如目录不可写、权限问题），捕获该错误并执行回退逻辑。
       - 回退逻辑：调用原有的 `adapter.inject_text`（由于 Plan 01 的修改，此处已自带限流和 \r）。
       - 打印信号发送方式（Bus 或 TTY Fallback）。
    2. 更新 `Commands::Wait` 处理逻辑:
       - 移除原有的 stdin 轮询代码。
       - 调用 `FileBus::recv`，传递 `timeout` 参数。
       - 如果接收到信号，打印 Payload。
  </action>
  <verify>
    <automated>cargo build && ./target/debug/vibe signal test_sig '{"ok":true}' && ./target/debug/vibe wait test_sig --timeout 1</automated>
  </verify>
  <done>CLI 命令已无缝切换到文件总线，并具备优雅回退能力。</done>
</task>

<task type="auto">
  <name>Task 2: 编写并运行端到端验证脚本</name>
  <files>scripts/verify_phase17.sh</files>
  <action>
    1. 创建 `scripts/verify_phase17.sh`。
    2. 脚本逻辑:
       - 确保当前环境在 WezTerm 或 Tmux 中。
       - 使用 `vibe spawn Worker` 启动一个 Worker。
       - 使用 `vibe inject` 向 Worker 注入一条会让其发送信号的命令（例如：`vibe signal task_done '{"status":"ready"}'`）。
       - 在主会话中运行 `vibe wait task_done` 并捕获输出。
       - 验证整个过程不需要用户按下任何物理按键。
    3. 赋予执行权限并运行。
  </action>
  <verify>
    <automated>bash scripts/verify_phase17.sh</automated>
  </verify>
  <done>端到端验证成功，证明了 100% 的自动化触发可靠性。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Shell -> CLI | 环境变量和命令行参数 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Denial of Service | FileBus | mitigate | 限制信号文件的生命周期，读取后即刻删除，防止磁盘空间占满 |
</threat_model>

<verification>
运行验证脚本，观察 Master 是否能在无需干预的情况下接收到 Worker 的信号。
</verification>

<success_criteria>
- `vibe signal` 在文件总线可用时优先使用总线。
- `vibe wait` 能够捕获到跨进程发送的信号负载。
- 验证脚本在无人值守的情况下成功完成 Master-Worker 闭环。
</success_criteria>

<output>
After completion, create `.planning/phases/17-bi-directional-flow-reliability/17-03-SUMMARY.md`
</output>
