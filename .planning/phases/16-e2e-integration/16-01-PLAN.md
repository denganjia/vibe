---
phase: 16-e2e-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["scripts/e2e_test.sh"]
autonomous: true
requirements: ["BUS-11"]
must_haves:
  truths:
    - "User can run the E2E script locally and see a SUCCESS result"
    - "Master agent spawns a Scanner agent without user intervention"
    - "Scanner agent reports success and signals completion"
    - "Master agent waits and correctly receives the signal"
  artifacts:
    - path: "scripts/e2e_test.sh"
      provides: "E2E verification logic"
      contains: "spawn Scanner"
  key_links:
    - from: "scripts/e2e_test.sh"
      to: "vibe spawn"
      via: "command execution"
      pattern: "vibe-cli spawn"
---

<objective>
Verify the full autonomous development workflow.

Purpose: To ensure the master agent can spawn a sub-agent, the sub-agent can execute tasks and signal back, and the master can receive the signal to continue the workflow.
Output: A passing `e2e_test.sh` script.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-e2e-integration/16-CONTEXT.md
@scripts/e2e_test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix E2E Test Script for WezTerm / CI environments</name>
  <files>scripts/e2e_test.sh</files>
  <action>The current E2E test script fails with a timeout because the spawned pane (in WezTerm or terminal) might not properly execute the mock script in non-interactive CI/background environments, or the WEZTERM executable is launching but the mock script isn't running correctly. 
Update `scripts/e2e_test.sh` to ensure `mock_agent.sh` executes reliably. Consider running the vibe wait loop and checking the output logs. Ensure that the test correctly handles the asynchronous nature of the spawn and wait mechanism, and that `mock_agent.sh` correctly resolves the path to `vibe-cli`.
If necessary, provide a dummy execution environment for `spawn` if a real terminal emulator isn't available during testing, or ensure the mock agent properly runs.</action>
  <verify>
    <automated>bash scripts/e2e_test.sh</automated>
  </verify>
  <done>Script exits with 0 and prints SUCCESS.</done>
</task>

</tasks>
