---
phase: 23
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified: [plugin/vibe/roles/Conductor.md, plugin/vibe/scripts/plan.js, .vibe/planning_notes.md]
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03]
must_haves:
  truths:
    - "Conductor asks clarifying questions if file_scope or verification is missing"
    - "Conductor generates multiple tasks with explicit 'dependencies' in task.json"
    - "User can read contextual notes in .vibe/planning_notes.md explaining the plan"
  artifacts:
    - path: "plugin/vibe/roles/Conductor.md"
      provides: "Conductor skill with clarification checklist"
    - path: "plugin/vibe/scripts/plan.js"
      provides: "Static plan generation logic"
  key_links:
    - from: "plugin/vibe/scripts/plan.js"
      to: ".vibe/plan.json"
      via: "plan manifest creation"
---

<objective>
实现 Conductor Skill 和 `plan.js` 脚本，具备保守澄清机制和静态拓扑拆分能力。

Purpose: 确保任务生成的质量（澄清充分）以及复杂工作流的可预览性。
Output: `Conductor.md` 角色定义和 `plan.js` 脚本。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/23-CONTEXT.md
@.planning/phases/23-RESEARCH.md
@.planning/phases/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Conductor role and clarification checklist</name>
  <files>plugin/vibe/roles/Conductor.md</files>
  <action>
    - 创建/更新 `plugin/vibe/roles/Conductor.md`。
    - 定义 Conductor 的职责：澄清需求 -> 拆分计划 -> 派发任务 -> 监控进度。
    - 引入保守计划检查清单（per D-01）：
        - 目标 (goal) 是否二元可验证？
        - 文件范围 (file_scope) 是否显式列出？
        - 验证命令 (verification) 是否可在当前环境运行？
    - 指导 Conductor 在不满足上述条件时发起对话，而非生成任务。
  </action>
  <verify>
    <automated>grep -E "goal|file_scope|verification" plugin/vibe/roles/Conductor.md</automated>
  </verify>
  <done>Conductor.md 包含明确的澄清指引和约束</done>
</task>

<task type="auto">
  <name>Task 2: Implement plan.js for static task generation</name>
  <files>plugin/vibe/scripts/plan.js, .vibe/planning_notes.md</files>
  <action>
    - 实现 `plugin/vibe/scripts/plan.js`：
        - 接收来自主模型的结构化计划描述（JSON 格式）。
        - 验证计划中任务的拓扑关系，防止循环依赖。
        - 生成 `.vibe/plan.json` 清单。
        - 生成 `.vibe/tasks/*.json` 文件，每个文件包含 `dependencies` 数组。
        - 将复杂的背景信息、用户指示或权衡说明写入 `.vibe/planning_notes.md`（per D-01）。
  </action>
  <verify>
    <automated>
      # 模拟生成计划
      node plugin/vibe/scripts/plan.js --mock-input='{"goal":"test", "tasks":[{"id":"T1", "dependencies":[]}, {"id":"T2", "dependencies":["T1"]}]}'
      [ -f .vibe/plan.json ] && [ -f .vibe/tasks/T1.json ] && [ -f .vibe/tasks/T2.json ]
    </automated>
  </verify>
  <done>plan.js 能根据主模型指令生成完整的静态任务集合和清单</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Conductor → Filesystem | Conductor writes task and planning notes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Information Disclosure | .vibe/planning_notes.md | mitigate | Ensure planning_notes.md is excluded from production distribution via .vibeignore or similar |
</threat_model>

<verification>
1. 提示主模型扮演 Conductor。
2. 给出模糊需求，验证 Conductor 是否发起澄清。
3. 给出明确需求，验证 Conductor 是否成功调用 `plan.js` 并生成任务和依赖关系。
</verification>

<success_criteria>
- Conductor 遵循保守计划原则。
- `plan.js` 能够输出符合拓扑结构的 `task.json` 文件。
- `planning_notes.md` 能够承载长上下文供后续 Executor 参考。
</success_criteria>

<output>
After completion, create `.planning/phases/23-多模型执行与审查闭环/23-02-SUMMARY.md`
</output>
