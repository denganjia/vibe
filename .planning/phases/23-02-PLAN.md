---
phase: 23
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified: [plugin/vibe/skills/Conductor.md, plugin/vibe/commands/plan.md, plugin/vibe/scripts/plan.js]
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03]
must_haves:
  truths:
    - "User can see a complete list of tasks and their order before execution starts."
    - "Conductor asks for clarification when the goal, scope, or verification are vague."
    - "Conductor refuses to proceed if a circular dependency is detected in the plan."
  artifacts:
    - path: "plugin/vibe/skills/Conductor.md"
      provides: "Agent role definition"
    - path: "plugin/vibe/scripts/plan.js"
      provides: "Plan generation logic"
    - path: "plugin/vibe/commands/plan.md"
      provides: "User-facing command interface"
  key_links:
    - from: "plugin/vibe/scripts/plan.js"
      to: ".vibe/plan.json"
      via: "fs.writeFileSync"
---

<objective>
Implement the Conductor's ability to clarify requirements and generate a structured execution plan. This enables the transition from a vague user goal to a set of executable tasks with dependencies.

Purpose: To provide the "brain" for multi-model orchestration.
Output: Conductor skill definition, planning script, and plan command.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-CONTEXT.md
@.planning/phases/23-RESEARCH.md
@plugin/vibe/references/agent-contract.md
@plugin/vibe/references/task-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Conductor Skill</name>
  <files>plugin/vibe/skills/Conductor.md</files>
  <action>
    - Create a Markdown skill file for the Conductor role.
    - Define the Operating Loop:
        1. **Clarify**: Must ensure `goal`, `file_scope`, and `verification` are explicit before planning (per D-01).
        2. **Plan**: Use `plan.js` to generate `plan.json`, `task.json` files, and `planning_notes.md`.
        3. **Execute**: Trigger execution via the `run-task` command.
    - Include instructions for using `planning_notes.md` to store important context (per D-01).
  </action>
  <verify>
    <automated>ls plugin/vibe/skills/Conductor.md</automated>
  </verify>
  <done>Conductor skill is defined with clear clarification and planning loops.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Planning Script (plan.js)</name>
  <files>plugin/vibe/scripts/plan.js</files>
  <action>
    - Create `plugin/vibe/scripts/plan.js` as a pure Node.js script.
    - Features:
        1. Accepts a plan goal and a JSON-structured list of tasks with dependencies.
        2. Generates `.vibe/plan.json` (manifest).
        3. Generates individual `.vibe/tasks/*.json` files.
        4. Generates `.vibe/planning_notes.md` with initial context.
        5. Performs an acyclicity check (DFS) to prevent circular dependencies (per Pitfall 1).
    - Reference D-02: Static topology generation.
  </action>
  <verify>
    <automated>
      node plugin/vibe/scripts/plan.js --goal "Build auth" --tasks '[{"id": "t1", "goal": "schema"}, {"id": "t2", "goal": "api", "dependencies": ["t1"]}]'
      ls .vibe/plan.json .vibe/tasks/t1.json .vibe/tasks/t2.json .vibe/planning_notes.md
    </automated>
  </verify>
  <done>plan.js can generate structured plans and validate dependency integrity.</done>
</task>

<task type="auto">
  <name>Task 3: Create Plan Command Contract</name>
  <files>plugin/vibe/commands/plan.md</files>
  <action>
    - Define the `plan` command in the plugin.
    - It must map to `node plugin/vibe/scripts/plan.js`.
    - Document the expected JSON format for tasks so the Conductor knows how to invoke it.
  </action>
  <verify>
    <automated>ls plugin/vibe/commands/plan.md</automated>
  </verify>
  <done>Plan command is documented for the agent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Input → plan.js | Malicious goal strings or task definitions. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | tasks/*.json | mitigate | Validate task data structure before writing. |
| T-23-04 | Information Disclosure | planning_notes.md | mitigate | Remind Conductor not to include secrets in notes. |
| T-23-09 | Denial of Service | plan.js | mitigate | Set a maximum depth for circular dependency detection. |
</threat_model>

<verification>
- Verify Conductor.md instructions align with CONTEXT.md decisions.
- Test `plan.js` with valid and circular dependency sets.
- Verify manifest and task files match expected schemas.
</verification>

<success_criteria>
- Conductor role has clear instructions for clarification and planning.
- `plan.js` successfully creates the plan manifest and tasks.
- Circular dependencies are detected and rejected.
- `planning_notes.md` is created as a reference.
</success_criteria>

<output>
After completion, create `.planning/phases/23-多模型执行与审查闭环/23-02-SUMMARY.md`
</output>
