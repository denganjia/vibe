---
phase: 06-status-dashboard-ux
plan: 01
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - crates/vibe-core/src/ipc/protocol.rs
  - crates/vibe-core/src/ipc/server.rs
  - apps/vibe-cli/src/main.rs
  - apps/vibe-cli/Cargo.toml
autonomous: true
requirements: ["MON-02"]

must_haves:
  truths:
    - "Master supports multiple TUI clients via UDS broadcasting"
    - "TUI displays a real-time table of all agents"
    - "Selected agent shows live log preview in bottom pane"
    - "Hotkeys (f, k, Enter) work as expected"
  artifacts:
    - path: "apps/vibe-cli/src/main.rs"
      provides: "vibe status (TUI)"
    - path: "crates/vibe-core/src/ipc/server.rs"
      provides: "Broadcast mechanism"
---

<objective>
构建全屏交互式的“指挥塔” TUI，实现全量 Agent 监控、实时日志预览及便捷物理调度。

目的：提供极高生产力的监控中心，实现物理级隔离与逻辑级耦合的直观可视化。
输出：增强的 UDS 广播协议、Ratatui TUI 看板、实时日志追踪组件。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 建立 UDS 订阅与广播机制</name>
  <files>crates/vibe-core/src/ipc/protocol.rs, crates/vibe-core/src/ipc/server.rs</files>
  <action>
    1. 协议扩展：新增 `Subscribe` (从 TUI 到 Master) 和 `Broadcast { states: Vec<WorkerState> }` 消息。
    2. 在 `MasterServer` 中引入 `BroadcastSubscribers` 管理器（基于 `Arc<Mutex<Vec<Sender>>>`）。
    3. 每当 Master 收到 `Heartbeat` 或 `Report` 时，触发一次全局广播。
  </action>
  <verify>
    <automated>cargo check (确保服务器逻辑编译通过)</automated>
  </verify>
  <done>Master 现在具备了“电台”功能，能向所有观察者推送状态。</done>
</task>

<task type="auto">
  <name>Task 2: 搭建 Ratatui TUI 基础框架</name>
  <files>apps/vibe-cli/Cargo.toml, apps/vibe-cli/src/main.rs</files>
  <action>
    1. 在 `vibe-cli` 中引入 `ratatui`, `crossterm` 依赖。
    2. 实现 `Commands::Status` 子命令，启动全屏 TUI 模式。
    3. 实现异步主循环：同时监听 `crossterm` 输入流和 Master UDS 推送流。
  </action>
  <verify>
    <automated>vibe status (应显示全屏空白界面且能按 q 退出)</automated>
  </verify>
  <done>TUI 的“骨架”与事件循环搭建完成。</done>
</task>

<task type="auto">
  <name>Task 3: 实现仪表盘布局与日志追踪</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    1. 绘制 UI 布局：
       - 上部：`Table` 组件显示 ID, Role, Status, Summary。
       - 下部：`Paragraph` 组件显示选中项的日志尾部。
    2. 实现日志追踪逻辑：每隔 500ms 或选中行改变时，异步读取 `logs/{vibe_id}.log` 最后 2KB 内容。
    3. 增加颜色渲染：根据状态（Running/Failed）对表格行进行着色。
  </action>
  <verify>
    <manual>运行几个任务，观察 vibe status 是否实时刷新且显示日志。</manual>
  </verify>
  <done>“指挥塔”的核心视觉与监控能力已交付。</done>
</task>

<task type="auto">
  <name>Task 4: 集成交互快捷键</name>
  <files>apps/vibe-cli/src/main.rs</files>
  <action>
    1. 绑定按键事件：
       - `f`: 调用 `vibe focus` 逻辑。
       - `k`: 向 Master 发送 `KillRequest`。
       - `Enter`: 物理切换焦点并退出 TUI。
    2. 实现列表选择同步：确保选中行与操作目标 ID 始终一致。
  </action>
  <verify>
    <manual>在 TUI 中按下 f 键，观察 Wezterm 窗格是否成功切换。</manual>
  </verify>
  <done>实现了“看、选、调”的一体化交互体验。</done>
</task>

</tasks>

<verification>
1. 启动 Master。
2. 运行多个 `vibe run` 模拟工作队列。
3. 运行 `vibe status`。
4. 确认列表刷新频率正常（< 1s）。
5. 确认选中项的日志实时更新。
6. 测试 f/k/Enter 快捷键的物理反馈。
</verification>

<output>
After completion, create .planning/phases/06-status-dashboard-ux/06-01-SUMMARY.md
</output>
