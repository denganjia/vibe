---
phase: 24
plan: 03
type: execute
wave: 3
depends_on: [24-02]
files_modified: [crates/vibe-core/src/ipc/bus.rs, crates/vibe-core/src/state/mod.rs, crates/vibe-core/Cargo.toml, apps/vibe-cli/Cargo.toml]
autonomous: true
requirements: [REL-05]
must_haves:
  truths:
    - "冗余的 Rust 模块（Bus, 旧 State/Role 逻辑）已被移动至 archive/ 目录"
    - "Cargo.toml 中不再包含已移除模块相关的依赖项"
    - "核心 Rust CLI 编译后的二进制体积显著减小"
  artifacts:
    - path: "archive/vibe-core/ipc/bus.rs"
      provides: "归档的总线逻辑"
---

<objective>
清理 `vibe-core` 中的冗余逻辑并归档旧代码，精简依赖项。

Purpose: 彻底落实插件化架构，移除 Rust 层面的重型逻辑，降低维护成本。
Output: 归档后的代码库和精简后的 `Cargo.toml`。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-CONTEXT.md
@.planning/phases/24-RESEARCH.md
@crates/vibe-core/src/state/mod.rs
@crates/vibe-core/Cargo.toml
@apps/vibe-cli/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 归档冗余模块</name>
  <files>crates/vibe-core/src/ipc/bus.rs, crates/vibe-core/src/state/mod.rs, archive/</files>
  <action>
    - 创建根目录下的 `archive/vibe-core` 文件夹。
    - 将 `crates/vibe-core/src/ipc/bus.rs` 移动至 `archive/vibe-core/ipc/bus.rs`。
    - 清理 `crates/vibe-core/src/state/mod.rs`：
        - 移除 `RoleManager`（已由插件 scripts 处理）。
        - 移除旧的 `ProjectConfig` 和 `ConfigManager`（已迁移至 `.vibe/config.json` 及相关脚本）。
        - 仅保留 `StateStore` 中与 `panes.json` 读写相关的极简逻辑，供分发器或基础适配器使用。
    - 将 `apps/vibe-cli/src/tui.rs` 移动至 `archive/vibe-cli/tui.rs`（如果分发器不再需要内置 TUI）。
  </action>
  <verify>
    <automated>ls archive/vibe-core/ipc/bus.rs</automated>
  </verify>
  <done>旧代码成功归档，核心库逻辑得到精简。</done>
</task>

<task type="auto">
  <name>Task 2: 清理依赖项</name>
  <files>crates/vibe-core/Cargo.toml, apps/vibe-cli/Cargo.toml</files>
  <action>
    - 从 `crates/vibe-core/Cargo.toml` 中移除：`dialoguer`, `chrono` (如果不涉及 panes.json 时间戳), `daemonize` (如果不再需要后台总线)。
    - 从 `apps/vibe-cli/Cargo.toml` 中移除：`ratatui`, `crossterm` (如果 TUI 已移除), `dialoguer`, `uuid` (如果不在 main.rs 生成 UUID)。
    - 运行 `cargo build` 确保核心逻辑仍然可用。
  </action>
  <verify>
    <automated>cargo build</automated>
  </verify>
  <done>依赖项清理完成，项目构建正常。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internal -> Archive | 代码移动操作 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | Cargo.toml cleanup | mitigate | 逐步清理依赖并频繁进行编译测试 |
</threat_model>

<verification>
1. 检查 `archive/` 目录结构。
2. 运行 `cargo build` 验证编译。
3. (可选) 检查生成的二进制文件大小。
</verification>

<success_criteria>
- 冗余逻辑被物理移除并妥善归档。
- 项目依赖项保持在最小必要集合。
</success_criteria>

<output>
After completion, create .planning/phases/24-cli-slimming-p2/24-03-SUMMARY.md
</output>
