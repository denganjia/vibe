---
phase: 24
plan: 03
type: execute
wave: 3
depends_on: [24-02]
files_modified: [crates/vibe-core/src/ipc/bus.rs, crates/vibe-core/src/state/mod.rs, crates/vibe-core/Cargo.toml, apps/vibe-cli/Cargo.toml]
autonomous: true
requirements: [REL-05]
must_haves:
  truths:
    - "核心代码库中不再包含冗余的 Bus 和旧版 State 管理逻辑"
    - "旧版业务逻辑（如 spawn 编排）已成功移动至 archive/ 目录且不影响编译"
    - "Cargo.toml 已精简，移除了不再使用的依赖项（如 ratatui, daemonize 等）"
  artifacts:
    - path: "archive/vibe-core/ipc/bus.rs"
      provides: "已归档的总线代码"
---

<objective>
彻底清理并归档 Rust 层面的冗余业务逻辑，精简项目依赖，完成 v6.0 的收束。

Purpose: 减少维护负担，降低二进制体积，确保代码库整洁。
Output: 精简后的代码库及 `archive/` 目录。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-CONTEXT.md
@.planning/phases/24-RESEARCH.md
@.planning/phases/24-VALIDATION.md
@crates/vibe-core/Cargo.toml
@apps/vibe-cli/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 代码归档与模块清理</name>
  <files>crates/vibe-core/src/ipc/bus.rs, crates/vibe-core/src/state/mod.rs, archive/</files>
  <action>
    - 创建 `archive/vibe-core/ipc` 和 `archive/vibe-core/state` 目录。
    - 将 `crates/vibe-core/src/ipc/bus.rs` 移动至 `archive/vibe-core/ipc/bus.rs`。
    - 重构 `crates/vibe-core/src/state/mod.rs`：
        - 移除 `RoleManager` 相关代码（逻辑已下沉至脚本）。
        - 移除旧的 `ProjectConfig` 相关逻辑。
        - 仅保留极简的 `StateStore` 接口供未来适配器使用。
    - 将 `apps/vibe-cli/src/tui.rs`（若存在且已弃用）移动至 `archive/vibe-cli/tui.rs`。
  </action>
  <verify>
    <automated>ls archive/vibe-core/ipc/bus.rs</automated>
  </verify>
  <done>旧代码逻辑物理隔离，核心库得到精简。</done>
</task>

<task type="auto">
  <name>Task 2: 依赖项精简与编译验证</name>
  <files>crates/vibe-core/Cargo.toml, apps/vibe-cli/Cargo.toml</files>
  <action>
    - 在 `crates/vibe-core/Cargo.toml` 中，移除 `daemonize`, `sqlx` (若已不用), `uuid` (若逻辑已迁移) 等依赖。
    - 在 `apps/vibe-cli/Cargo.toml` 中，移除 `ratatui`, `crossterm`, `dialoguer` 等 TUI/交互相关依赖。
    - 运行 `cargo build` 确保精简后的项目依然能通过编译。
  </action>
  <verify>
    <automated>cargo build</automated>
  </verify>
  <done>依赖项清理完成，项目构建正常，且体积理论上有所减小。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internal -> Archive | 代码资产的移动 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | Build configuration | mitigate | 每次移除依赖后立即执行编译，确保无遗漏引用 |
</threat_model>

<verification>
1. 运行 `ls -R archive/` 验证归档结构。
2. 运行 `cargo build` 确保编译通过。
</verification>

<success_criteria>
- 所有已确认冗余的 Rust 模块均已进入 `archive/` 或被移除。
- 项目依赖项被压缩至最小必要集合。
- 最终产出的 vibe 二进制文件仍能维持基础分发功能。
</success_criteria>

<output>
After completion, create .planning/phases/24-cli-slimming-p2/24-03-SUMMARY.md
</output>
