# Vibe Examples

## Included After Phase 20

Examples added after Phase 20 should demonstrate task contracts, Agent definitions, review artifacts, lock behavior, run records, and release summary drafts.

## Example Boundaries

Examples are model-readable demonstrations of contracts. They must not be treated as installed user workspace state, and scripts must not copy them over project `.vibe` files unless a future init command explicitly asks for example material.
